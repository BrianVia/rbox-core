## Deliverable 1 — Round 1 — codex

- **HIGH — P-1 violation: an `unlinkDir` subtree rescan can publish an unstable child as a deletion.** `applyWatchEvents` calls the now-deferring `walk` without a `deferred` set when re-deriving a still-present directory (`src/engine/manifest.ts:151`). If a child changes between stat and hash, `drainHashes` silently omits it because no set was supplied (`src/engine/manifest.ts:369-373`); the authoritative-fresh-set cleanup then deletes the child's prior coherent entry (`src/engine/manifest.ts:153-157`). A stale `unlinkDir` event followed by a daemon push can therefore turn append/replace/chmod churn into a remote deletion. Thread a deferred set through this subtree walk, preserve prior entries for deferred children, and schedule the existing retry path. Add a deterministic daemon/incremental regression covering this exact ordering.

- **HIGH — P-1 violation: post-pull scan deferral can clobber the version just pulled.** After `pull()` applies remote content and advances the persisted sync base, `doPull` calls `replaceManifestFromScan(this.cache, this.manifest)` with the pre-pull in-memory manifest as the carry source (`src/cli/daemon.ts:794`; helper at `src/cli/daemon.ts:1162-1166`). If a remotely changed path is unstable during that rescan, the helper restores its stale pre-pull entry (or pre-pull absence). The pump's follow-up push can then interpret that stale tuple/deletion as a local change against the new base and overwrite the remote version before the 200 ms retry runs. This site must carry from coherent post-pull truth (for example the newly loaded `lastSyncedManifest`) or suppress pushing until re-derivation succeeds. Add a deterministic post-pull defer-then-push regression.

- **MEDIUM — HashCache v2 accepts corrupt entries inside an otherwise valid envelope.** `HashCache.load` validates `{version: 2, entries: object}` but does not validate each entry (`src/engine/hashcache.ts:82-90`). A syntactically valid damaged entry with matching numeric `(mtimeMs,size,ctimeMs)` and an arbitrary/malformed `sha256` is accepted by `lookup` (`src/engine/hashcache.ts:41-44`). Since the cache is specified as safe-to-discard and corruption must cost only a re-hash, validate v2 entries (including finite fingerprint fields and a valid SHA-256) and discard the cache wholesale on any malformed entry. The current corruption test covers invalid JSON only.

- **LOW — the restored-mtime stale-SHA gate does not assert its ctime premise.** The test proves equal size/mtime and verifies the new SHA (`src/engine/engine-m1.test.ts:183-205`), but never asserts `st2.ctimeMs !== st1.ctimeMs`. On a filesystem with coarse or surprising ctime behavior this can fail nondeterministically without identifying why. Assert the ctime change (or use a deterministic stat seam) so the test explicitly gates the P-2 fingerprint dimension it names.

- **PASS — required direct gates and ordinary production call sites.** The append, same-size atomic replacement with restored mtime, and chmod-at-hash full-scan cases deterministically defer and emit no mixed tuple; the shared predicate covers file type, ino, dev, size, mtime, ctime, and mode; the foreground push test carries the base entry; same-size write plus restored mtime re-hashes; legacy bare-map cache data is discarded and rewritten as v2. Both direct `HashCache.lookup` and `record` production call sites pass ctime/post-stat values, and the foreground push plus retry rescans thread scan deferrals. Targeted tests passed (35/35) and typecheck passed. The full `bun test ./src/` run encountered unrelated failures in `git-state.test.ts` and the documented `sync.test.ts` metadata-heal case before it was stopped.

Verdict: REVISE

## Deliverable 1 — Round 1 — revision (Claude)

All four findings accepted; dispositions:

1. **HIGH (unlinkDir subtree rescan deletes an unstable child) — ACCEPTED.** `applyWatchEvents`' still-a-dir `unlinkDir` branch now walks with its own deferred set: a child absent from the fresh walk because it was DEFERRED keeps its prior entry (excluded from the authoritative-subtree cleanup) and is surfaced to the caller's deferred set for the existing write-finish retry. The `addDir` branch threads the caller's deferred set through its subtree walk too. Regression: stale `unlinkDir` + churn-at-hash on the child → prior entry retained + path deferred (fails on the pre-fix code, verified by stash).
2. **HIGH (post-pull carry source is pre-pull truth) — ACCEPTED.** `doPull` now carries deferred paths from the freshly loaded POST-pull `lastSyncedManifest` (one `loadSyncBase` call also feeds `lastLoggedSeq`), never the pre-pull in-memory manifest. Regression (daemon-scan-defer.test.ts) uses the sharp variant — a freshly PULLED file deferred during the post-pull rescan: carried-from-pre-pull it is ABSENT and the chained push commits its deletion with no blob upload to catch it (the modified-file variant is masked by upload-time churn deferral). Fails without the fix (head advances to 3, g.txt deleted), passes with it (verified by stash both ways).
3. **MEDIUM (v2 envelope accepts corrupt entries) — ACCEPTED.** `HashCache.load` validates every entry (finite mtimeMs/size/ctimeMs, 64-hex sha256); ANY malformed entry discards the whole file — wholesale by contract, a damaged sha must never flow into a manifest. Test covers bad sha, wrong type, legacy shape inside a v2 envelope, and null.
4. **LOW (touch-r gate does not assert its ctime premise) — ACCEPTED.** The test now asserts `st2.ctimeMs !== st1.ctimeMs` alongside the restored-mtime/size equalities, so a coarse-ctime filesystem fails identifiably at the premise.

## Deliverable 1 — Round 2 — codex

- **HIGH — P-1 remains unsafe on the daemon's inner `pull()` scan, before the repaired post-pull carry runs.** `doPull` calls shared `pull()`, whose pre-apply `scanManifest` still supplies no `deferred` set (`src/cli/sync.ts:245`). Consequently `drainHashes` silently omits any file that changes across hash, and that incomplete manifest is immediately passed to three-way `reconcile` as authoritative local truth (`src/cli/sync.ts:269`). This can misclassify a previously synced churning path as a local deletion, and a never-synced churning path that collides with a remote path as absent, allowing the pull apply to overwrite it. Loading the post-pull base and carrying it in `doPull` at `daemon.ts:796` is too late: the destructive/conflict action may already have been planned and applied inside `pull()`. Thread scan deferrals through `pull()` and make reconciliation fail closed for unstable paths (base carry suffices only for previously synced paths; a deferred path absent from the base must also be protected from remote write/delete), with a deterministic daemon pull regression that churns during the **pre-apply** scan. The new `daemon-scan-defer` test explicitly leaves that scan unperturbed and therefore pins only the repaired post-pull rescan.

- **PASS — the four Round-1 edits themselves are locally sound.** The still-present `unlinkDir` branch preserves prior entries for deferred descendants and forwards them to the daemon retry set; `addDir` forwards deferrals and its merge cannot delete an existing entry. Startup, ignore-rule rebuild, `doFullScan`, and `doDeepScan` now carry coherent prior manifest entries, while `doPull`'s post-apply rescan correctly carries the freshly loaded post-pull base. HashCache v2 rejects malformed envelopes/entries wholesale with finite fingerprint fields and a lowercase 64-hex SHA. The unlinkDir and post-pull regressions exercise the specific repaired failures, and the focused Round-2 run passed 36/36 tests. However, no regression covers the remaining pre-apply pull hole above, so P-1 is not complete end-to-end.

Verdict: REVISE

## Deliverable 1 — Round 2 — revision (Claude)

**HIGH (pull pre-apply scan) — REBUTTED with evidence, plus pinning regressions.** The claimed hazards do not exist in the code:

- *"Misclassify a previously synced churning path as a local deletion"* — reconcile plans a `delete` action ONLY from `sameContent(l, b)` with a remote-side deletion (`src/engine/reconcile.ts:62-64`); a dropped path (`l` absent, `b` present) fails that equality, and with `r == b` it takes the `sameContent(r, b) → continue` no-action arm. There is no branch that converts local absence into a planned deletion, and pull persists the REMOTE manifest as the new base — local-scan incompleteness never lands in state.
- *"Allowing the pull apply to overwrite it"* — factually wrong: every `write` planned against an absent `expectedLocal` re-checks the target at publish time (`src/engine/apply.ts:221-227`); a churning file present on disk fails `sameContent(current, undefined)` and is MOVED ASIDE to a visible conflict copy before the remote bytes land. `deleteEntry` has the same mismatch-→-move-aside guard. No branch loses bytes.
- Outcome equivalence: pre-P-1, the same churn produced a torn local entry whose sha differed from base and remote → reconcile `conflict` → keep-local copy + remote applied. Post-P-1, the dropped entry produces write-with-absent-expectedLocal → apply's precondition check → conflict copy + remote applied. Same preserved bytes, same published truth; the never-synced-collision case is identical.

Evidence added (sync-scan-defer.test.ts): (1) churn during the pre-apply scan with remote UNCHANGED → zero actions, local mid-write bytes untouched; (2) churn with remote CHANGED → remote applied AND the mid-write bytes survive as a `.conflict` copy. Both use the deterministic hash-mock churn injection the review asked for. The SPEC-1 decision "pull sites pass no deferred set" stands, now pinned by tests rather than argument.

## Deliverable 1 — Round 3 — codex

- **PASS — the pull pre-apply rebuttal is correct under exhaustive reconciliation.** For a scan-dropped path `l === undefined`: if `r` is absent, the initial `sameContent(l, r)` arm exits with no action (regardless of `b`); if `r` is present and `b` is absent, `sameContent(l, b)` plans a `write` with absent `expectedLocal`; if `r === b`, the remote-unchanged arm exits; and if present `r !== b`, the final `r && !l` arm plans the same guarded `write`. A `delete` is unreachable: its `sameContent(l, b) && !r` condition is preceded by, and subsumed by, `sameContent(l, r)` when both are absent.

- **PASS — apply preserves bytes for every action reachable from a dropped path.** `writeEntry` stages and verifies the remote entry first, hashes the current target, and moves any present file/symlink aside whenever absent `expectedLocal` mismatches it before publishing. Ancestor file/symlink obstructions are likewise pre-staged then moved aside; target directories are preserved through trash or a visible conflict move. `deleteEntry` removes only a target matching `expectedLocal` and otherwise moves it aside, though reconcile cannot plan a delete for the dropped-path state above. No reconcile/apply branch was found that loses the deferred local bytes or plans their deletion.

- **PASS — the two pull regressions are non-vacuous end-to-end safety pins.** Their append injection necessarily changes the captured size, so current `drainHashes` drops the path; the unchanged-remote case proves the injection ran via exact final `one-edit` bytes and asserts zero actions, while the changed-remote case explicitly proves injection consumption, remote publication, conflict-copy creation, and exact preservation of `one-edit`. They would catch the alleged deletion and overwrite failures. They do not explicitly assert the internal omission/action shape (the changed case could additionally assert `write` with absent `expectedLocal`), but that is a pin-strength improvement, not a safety hole: the deterministic size mismatch establishes omission in the implementation under test and the asserted outcomes cover the contract.

- **PASS — design 85 §3.0 P-1/P-2 remains satisfied by `1b45a65c..HEAD`.** P-1 uses one shared file-type/ino/dev/size/mtime/ctime/mode predicate in incremental and batched full-scan hashing, records only post-hash coherent tuples, surfaces mismatches, and carries prior coherent truth at publishing/retry and daemon full-scan sites (including the repaired `unlinkDir` subtree and post-pull rescan). P-2 keys lookup/record on `(mtime,size,ctime)`, persists a v2 envelope, discards legacy or malformed data wholesale, and has the required restored-mtime and format/corruption gates. The focused suite passed 40/40 tests across the five P-1/P-2 and daemon/CLI regression files.

Verdict: ALIGNED

## Deliverable 2 — Round 1 — codex

- **BLOCKER — §6.1's scan `recordDetails` contract is absent.** `ScanStats` still has only the pre-deliverable fields (`src/engine/manifest.ts:32-55`), and the existing CLI report sites still spread that unchanged object (`src/cli/sync.ts:212,245,405`). There is no `residualMs`, `midwriteDeferred`, `dirsReusedFromCache`, or `dircache` detail anywhere. Computing residual only in the two daemon text lines (`src/cli/daemon.ts:80-83`) satisfies the §6.2 site line, but not §6.1; with `RBOX_METRICS` now default-on, ordinary CLI phase summaries expose the incomplete contract by default.

- **HIGH — a covering event can race the settle-to-sidecar handoff and become a false confirmed drop.** `runDriftAuditNow` removes the audit from `openDriftAudits` and snapshots `pendingEvents` before doing asynchronous per-candidate re-verification and the serialized sidecar append (`src/cli/daemon.ts:1334-1348`). A raw event arriving after that removal is not buffered into the audit; if its settled batch applies before the survivor is appended, event-apply-time resolution sees no pending candidate. The candidate is then persisted with no remaining evidence of the event and can be `confirmed` at the next deep scan. This violates filters (a)/(d), R4 F3/R5 F1/R6 F4, and Round 7 closure item 1. The transition from open audit to pending candidate must be atomic with event capture/application (or retain a generation/evidence barrier that apply-time resolution consumes).

- **HIGH — next-horizon resolution has the analogous late-boundary false-confirm path.** Old pending candidates are classified and removed from the sidecar immediately after the fresh scan using one coverage snapshot (`src/cli/daemon.ts:1236-1259`), while the current scan's raw-event window remains open for another four seconds (`src/cli/daemon.ts:1265,1320-1350`). A covering event delivered after that snapshot cannot resolve the already-removed candidate at apply time, yet the candidate has already incremented `confirmed`. The whole continuously observed horizon must close under the same settle/event-apply evidence before confirmation.

- **HIGH — the quiescent-only gate is mislabelled.** Candidates do not persist their origin scan's quiescence (`src/cli/drift-audit.ts:12-24`), but §5 permits only confirmed drops from quiescent-tagged scans to feed the gate. `confirmed` counts candidates created at scan N while the emitted `quiescent` value is computed from scan N+1's current audit window (`src/cli/daemon.ts:1349-1350`). Thus a candidate born during a non-quiescent scan can later be emitted as `confirmed=1 quiescent=y` and incorrectly feed the gate. Persist the origin tag (or maintain separate confirmed-by-origin-quiescence counts).

- **HIGH — always-on measurement sidecar failures can fail real sync work.** `mutateDriftState` propagates load/save transaction failures to its caller (`src/cli/daemon.ts:1299-1306`), and both settled watch-event branches await it after re-deriving the manifest (`src/cli/daemon.ts:729-750`). A drift-audit write failure (disk full, permissions, atomic-write failure) therefore rejects `applyPendingWatchEvents` and fails the daemon pump even though this deliverable is measurement-only and the sidecar is specified safe-to-discard. Deep scans have the same propagation path. Instrumentation persistence must fail soft without changing reconcile/apply/push behavior, while keeping in-memory state coherent after a failed write.

- **MEDIUM — pending-state growth is neither deduplicated nor bounded.** Survivors are appended blindly (`src/cli/daemon.ts:1348`). An older same-path candidate retained in `awaitingApply` can coexist with a new same-path candidate, and repeated horizons can grow/double-count the path. The loader likewise accepts an arbitrarily large valid array (`src/cli/drift-audit.ts:77-98`). Add path/horizon deduplication and a conservative cap/fail-soft policy so a soak sidecar cannot grow without bound or amplify hot-path work.

- **PASS — the remaining audited surfaces align.** P0.2 uses equal `(mtimeMs,ctimeMs)` plus the strict prior-scan-start-minus-2000ms test (`src/cli/scan-probe.ts:25-30`), matching §3.1's racy-clean discipline. The probe fields and weighted eligible readdir sum are present. Event-apply-time comparison is wired after both ignore-rebuild/full-scan and ordinary incremental branches, uses applied manifest truth, skips exact deferred candidates, and implements exact/subtree coverage. Newly added scan/probe/drift emitted lines contain counts/timings/booleans only; no raw path was found. The explicit `RBOX_METRICS=0|false` opt-out is correct, and no new JSON-capable command output pollution was found, but the default-on CLI/daemon noise is now materially larger as intended by SPEC-2 and makes the missing §6.1 details user-visible.

Verdict: REVISE

## Deliverable 2 — Round 1 — revision (Claude)

Dispositions:

1. **BLOCKER (§6.1 recordDetails absent) — PARTIALLY ACCEPTED.** The CLI scan details now carry `residualMs` (wall − readdir+stat+matcher+hash+sort, computed per site) and `midwriteDeferred` (the P-1 deferral count; pull sites pass a counting-only sink — deliberately still no carry, per the deliverable-1 pinned semantics); `formatScanStats` renders both; the two §35 report tests pin the new key set. `dirsReusedFromCache` and `dircache:` are NOT added: they describe Layer A's dircache, which does not exist — constant placeholder fields would be dead options. Recorded as a deviation for the PR body.
2. **HIGH (settle-to-sidecar race) — ACCEPTED.** `runDriftAuditNow` no longer closes the audit before persisting: re-verification runs while the audit's raw-event buffer is still live, and the decision section — final coverage re-check against rawEvents+pendingEvents+deferredRetryPaths, quiescence, horizon resolution, `mergePending`, `openDriftAudits.delete` — is one SYNCHRONOUS block inside the serialized sidecar transaction. After it, the survivor is already in the in-memory pending set that apply-time resolution reads, so no covering event can fall between "decided" and "resolvable".
3. **HIGH (horizon confirm decided at scan time) — ACCEPTED.** Scan time now only stashes per-pending-candidate fresh-disk snapshots (`horizonInputs`, a superset since apply-time only removes); confirm/revert/unattributable are decided in the settle transaction under the same evidence barrier, with covered candidates held pending. An overlapping audit's survivor (no stashed input) holds rather than misclassifies.
4. **HIGH (quiescence provenance) — ACCEPTED.** Candidates persist `quiescentAtScan` (stamped at settle, when quiescence is known); the drift line adds `confirmedQuiescent=` so only quiescent-origin confirms feed the gate. Loader validates the field. Regression: non-quiescent-origin candidate confirms with `confirmedQuiescent=0`.
5. **HIGH (sidecar failures fail the pump) — ACCEPTED.** `mutateDriftState` never throws: load already fails soft, save failures log once (reset on success) and keep in-memory state coherent; `runDriftAuditNow` is fully try/caught; the scan-probe save is caught at its call site. Regression: a directory squatting on the sidecar path — `applyPendingWatchEvents` does not throw, the covering event still resolves in memory.
6. **MEDIUM (growth unbounded) — ACCEPTED.** `mergePending` dedups by path (the OLDER candidate wins — original drop evidence and age) and caps at `PENDING_CAP` (500); the loader caps too. Unit-tested.

## Deliverable 2 — Round 2 — codex

- **HIGH — the atomic settle transaction still loses a covering event that was raw before the audit opened and is fully applied before settle.** Exact interleaving: (1) `onRawEvent(E)` increments `watcherUnsettledGeneration` while no audit is open, so `E` enters no `audit.rawEvents`; (2) the deep scan opens audit A, records `eventGenAtScan`, and finds the mutation as candidate C; (3) the watcher's settled callback puts E in `pendingEvents`, the pump begins the next push, and `applyPendingWatchEvents` removes E from `pendingEvents`; (4) its manifest derivation completes and `resolveDriftFromAppliedEvents(E)` runs on the serialized `driftIo` chain while C is not pending yet, so it resolves nothing; (5) at A's settle, E is in neither `audit.rawEvents`, `pendingEvents`, nor `deferredRetryPaths`, re-verification still sees the mutation, and the transaction appends C; (6) no evidence of E remains, so the next horizon can report C as `confirmed`. The inverse overlap is sound: if settle wins the chain, C is installed before apply-time resolution; and a raw event delivered while A is open is caught by A's buffer. The missing case is specifically pre-open raw evidence that drains before settle. `eventGenAtScan` is persisted but never used, and the pending queue/generation state is not snapshotted at scan start as the normative design requires. Preserve start-boundary evidence (for example a scan-start pending/generation barrier that settlement consumes), and add a deterministic regression for the ordering above.

- **MEDIUM — load-time cap does not perform the promised path deduplication and can double-count a valid duplicate sidecar.** `loadDriftAudit` validates and slices `pending`, but does not pass it through a path deduper (`src/cli/drift-audit.ts:95-117`). Thus a syntactically valid sidecar containing two candidates for one path survives load; apply resolution increments a class twice, or horizon resolution can report two confirmations for one path. `mergePending` prevents newly generated duplicates, but it does not repair duplicates already loaded, and its `new Map(held.map(...))` keeps the last value rather than selecting the older evidence. Deduplicate loaded state by path with an explicit oldest-candidate rule before applying the cap.

- **PASS — settle-time horizon resolution and overlap holding are otherwise sound.** `horizonInputs` snapshots every candidate pending at this scan's completion. Since event application only removes pending entries, that map is a superset of the subset remaining when this audit settles. A survivor introduced by an overlapping audit after the snapshot has no map entry and is held; whichever audit ordering occurs, neither can classify the other's unstashed survivor. Covered old candidates are also held for binding apply-time truth rather than confirmed from scan-time truth.

- **PASS — quiescence provenance is now correctly separated.** A survivor receives `quiescentAtScan` from its own audit's raw-event window, the field is validated on load, and later confirmation increments `confirmedQuiescent` only from that persisted origin tag. The regression proves a non-quiescent-origin candidate may confirm but cannot enter the quiescent-only count.

- **PASS — measurement persistence is fail-soft on the audited IO paths.** Drift load is safe-to-discard, drift save failure is swallowed after retaining coherent in-memory mutation, apply-time drift resolution therefore cannot reject the pump operation, `runDriftAuditNow` catches re-verification/audit failures, and scan-probe load/save failures are contained. Ordinary manifest scan/hash failures remain real pump failures, appropriately, because those are sync truth IO rather than measurement-sidecar IO.

- **PASS — the cap itself is conservative, and the §6.1 partial is acceptable for this deliverable.** `mergePending` bounds newly merged state at 500 and gives held evidence priority; the loader bounds work at 500 (subject to the dedup defect above). `residualMs` and `midwriteDeferred` are now present at all CLI scan report sites and pinned by the report tests. Omitting `dirsReusedFromCache` and `dircache` is an acceptable explicit deviation here because SPEC-2 simultaneously mandates measurement-only code and expressly forbids implementing the Layer-A dircache; constant placeholders would not measure anything. Those fields remain work for the Layer-A deliverable, not a blocker to P0 measurement.

- **VALIDATION —** `git diff --check 6f5a5741..HEAD` passed. The focused measurement plus sync run passed 59/60; its sole failure was the acceptance-listed pre-existing same-SHA metadata-heal test (`sync.test.ts:276`). No unrelated file was modified by this review.

Verdict: REVISE

## Deliverable 2 — Round 2 — revision (Claude)

Both findings accepted; dispositions:

1. **HIGH (pre-open raw evidence drained before settle) — ACCEPTED.** `OpenDriftAudit` gains an `appliedEvents` buffer: every settled batch that `applyPendingWatchEvents` drains while a window is open is captured into it (same cap/overflow discipline), and settle coverage unions `rawEvents ∪ appliedEvents ∪ pendingEvents ∪ deferredRetryPaths`. The exact reviewed interleaving — raw event before the scan opens, candidate created by the scan, batch applied before settle while nothing is pending — now resolves `racing`. Deliberately coverage-only: quiescence stays rawEvents-based, because an applied batch's raw events may predate the window (marking the scan non-quiescent for pre-window churn would be a mislabel; a batch whose raw events DID fall in the window is already counted by rawEvents). Regression pins the ordering and fails on the pre-fix code (stash-verified).
2. **MEDIUM (loader dedup / last-wins merge) — ACCEPTED.** `dedupePending` (oldest-per-path, then cap) is applied both on load and inside `mergePending`, so a duplicate-path sidecar cannot double-count resolutions or confirms and the oldest evidence always wins. Unit-tested for load and merge.

## Deliverable 2 — Round 3 — codex

- **HIGH — measurement failure logs can emit raw absolute paths.** The normal scan-probe and drift summary lines are bounded and path-free, but the new fail-soft paths interpolate the underlying filesystem exception verbatim: scan-probe save failure at `src/cli/daemon.ts:1295`, drift sidecar load/save failure at `src/cli/daemon.ts:1316`, and the audit catch at `src/cli/daemon.ts:1408`. Node filesystem errors normally include the operated absolute pathname, so a directory squatting on `.rbox/state/drift-audit.json`, a permission error, or an atomic-write failure can place the workspace root/raw sidecar path in the daemon log. This violates §5 P0.3's bounded non-PII line requirement and the deliverable's no-raw-path emission contract. Log a fixed message plus a sanitized code/type, not `e.message`/`String(e)`; pin the failure regressions to reject the workspace root and sidecar pathname.

- **HIGH — pending drift evidence adds an O(manifest-size) pass/allocation to every settled watch batch.** Both incremental branches synchronously await `resolveDriftFromAppliedEvents` (`src/cli/daemon.ts:763,767`). Whenever even one candidate is pending, `resolveCoveredAtApply` first builds `new Map(manifest.files.map(...))` over the entire manifest (`src/cli/drift-audit.ts:74-80`) before examining the capped candidate set. A candidate can remain pending for the full 30-minute audit horizon, so ordinary watcher-driven pump operations repeatedly pay a second full-tree traversal and allocation even though event-time truth is needed only for covered candidates (at most 500). That is a material pump-hot-path regression against the measurement-only/no-behavior-cost intent. Resolve coverage first and look up only covered candidate paths (for example by binary search in the sorted manifest or a maintained path index), without rebuilding a whole-manifest map per batch.

- **PASS — the Round 2 watcher-evidence fix closes the reported interleaving with no remaining false-confirm ordering.** `applyPendingWatchEvents` copies every drained batch into each open audit before its first await (`src/cli/daemon.ts:745-755`), and settle unions `rawEvents`, `appliedEvents`, still-pending events, and deferred paths (`src/cli/daemon.ts:1364-1370`). Thus pre-open raw evidence drained before settle classifies the candidate `racing`. If settle wins instead, survivor installation and audit removal occur in the same synchronous serialized decision (`src/cli/daemon.ts:1396-1403`), after which apply-time resolution sees the pending candidate. Raw arrivals during asynchronous re-verification remain buffered. Keeping quiescence based only on `rawEvents` is safe: an event whose raw callback occurred in-window is already present there, while a pre-window raw event applied in-window is coverage evidence but not scan-window churn. Apply failure/overflow can only conservatively race or hold evidence, not falsely confirm it. The deterministic regression covers the formerly failing ordering.

- **PASS — load and merge deduplication are oldest-per-path and bounded.** `dedupePending` explicitly retains the lower `firstSeenAtMs` and caps the unique-path result at 500 (`src/cli/drift-audit.ts:85-94`); both `mergePending` and `loadDriftAudit` pass through it (`src/cli/drift-audit.ts:96-99,103-127`). The load/merge tests cover duplicate order, oldest-wins, and the cap. No remaining double-count or last-wins path was found.

- **VALIDATION —** `git diff --check 6f5a5741..HEAD` passed. The focused measurement plus sync run passed 61/62; the sole failure remains the acceptance-listed pre-existing same-SHA metadata-heal test at `src/cli/sync.test.ts:276`. No file other than this review was modified by Round 3.

Verdict: REVISE
