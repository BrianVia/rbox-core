1. **BLOCKER — `finishD1` does not fence repair audits created after purge completion.** Repair execute unconditionally inserts its full `attempted` audit and permits only `1/1` or `1/0` vectors ([design 180 §6](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1110)). The purge protection only covers audits serialized before `finishD1`; afterward the deletion ledger is already `done`, so a later platform dry-run or execute can insert an account-linked row containing operator, reason, observations, and possibly claim bytes that no future purge will scrub. This contradicts the claim that the full row is account-scoped only until `finishD1` ([design 180](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1202)) and design 37’s completed-erasure boundary ([design 37](/home/via/Development/Personal/rbox-core/docs/design/37-account-deletion.md:318)). Audit creation must transactionally exclude a purging/done deletion ledger—or immediately create only the permitted minimized record—and define the resulting vector/response. Add reverse-order races where `finishD1` wins before execute and dry-run audit insertion; neither may leave an unscrubbed account-linked row.

2. **HIGH — direct pairing and recovery remain able to overwrite journal-bound enrollment material.** The mandatory pending-state consumers omit direct `rbox connect`/pair redemption and `rbox key recover`, and the whole-command matrix omits them too ([design 180 §4](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:844), [tests](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1670)). This is especially exposed by design 179’s Keychain restore, which proceeds into admission persistence after phrase validation ([design 179](/home/via/Development/Personal/rbox-core/docs/design/179-recovery-kit-macos-keychain.md:121)). Those paths do not necessarily cross the proposed E2EE-loading gate: current recovery/pairing persists new `device.json`/`mk.key` before admission ([e2ee-client.ts](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:140), [recovery path](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:202)). With an active journal, this destroys the exact local keys bound to that journal and forces `integrity-failure`. Specify pending-state arbitration under the genesis lock before manual/Keychain recovery or pairing can replace credentials, persist keys, or admit, and add whole-command zero-side-effect tests for both entry points.

Verdict: **CHANGES-REQUIRED**

WORK-COMPLETE