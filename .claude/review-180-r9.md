1. **BLOCKER — workspace creation can resurrect an account-linked audit row after erasure.** The bootstrap and two workspace-row deletion-ledger guards are folded, but `createWorkspace` currently performs the guarded ownership/fair-use batch and then separately calls `audit` ([authz.ts:93](/home/via/Development/Personal/rbox-core/apps/api/src/authz.ts:93), [authz.ts:104](/home/via/Development/Personal/rbox-core/apps/api/src/authz.ts:104)). That audit performs an unconditional `audit_log` insert, and the table has no account foreign key ([authz.ts:114](/home/via/Development/Personal/rbox-core/apps/api/src/authz.ts:114), [0006_tenancy.sql:35](/home/via/Development/Personal/rbox-core/apps/api/migrations/0006_tenancy.sql:35)). Therefore: creation batch commits → `finishD1` deletes account audit rows and marks the ledger `done` → delayed creation audit inserts a new account-linked row. This violates invariant 12’s “cannot recreate an account-linked row” guarantee ([design 180:314](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:314)). The design must require the creation audit either inside the guarded transaction or behind its own statement-local deletion-ledger guard, and add the mid-operation reverse race; the existing test only schedules `finishD1` before workspace creation begins ([design 180:1970](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1970)).

2. **HIGH — global pairing arbitration does not close the critical section around all competing local state.** The no-account-scoped pre-redemption rule and global enumeration are folded, but only active-journal publication/final retirement coordinate with the global lock, and pairing is required to hold it only through `/pair/redeem` ([design 180:925](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:925), [design 180:941](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:941)). A prepublication marker can therefore appear after the final scan despite the test matrix promising marker-only state blocks redemption. Moreover, after the consuming response, pairing still fetches/verifies, replaces credentials, writes device/MK, and performs admission ([e2ee-client.ts:166](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:166), [e2ee-client.ts:189](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:189)); a new target-account pending state can appear during that interval. Require the first pending-artifact publication, including the prepublish marker, to coordinate with the global lock, then either retain that lock through pairing completion or atomically hand off under global→target-account lock before local/admission mutation. Add races for marker publication after the empty scan and journal publication after the redeem response but before credential/device/admission writes; the current race ends at token consumption ([design 180:1878](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1878)).

**Verdict: CHANGES-REQUIRED**

WORK-COMPLETE