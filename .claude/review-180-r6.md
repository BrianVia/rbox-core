## Verdict: CHANGES-REQUIRED

The file/line claims checked against the repository are accurate. Round-5 folds 1–4 and 7–8 close their original findings. Fold 5 remains incomplete, and fold 6 lacks full failure-path coverage.

1. **BLOCKER — Reconcile-before-tombstone-mutation is not race-safe.**  
   [Design 180](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:235) requires every attempted audit to be completed before claim mutation, but the repair-bootstrap predicate at [line 841](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:841) does not guard against new `outcome='attempted'` rows appearing after reconciliation. The same applies to account purge. For example: purge reconciles; admin execute atomically installs a tombstone but crashes before audit completion; purge deletes `account_keys`; reconciliation can no longer distinguish that successful execute from `update=0`. Reconciliation and mutation must share one transaction, or every mutation must condition on zero attempted audits and retry. Add interleaving tests for bootstrap, hard account purge, and future cancel/re-repair. Round-5 finding 5 is therefore not closed.

2. **HIGH — Crashed dry-run audits are mishandled by the stated reconciler.**  
   Dry-runs also begin as `attempted` ([line 1121](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1121)), but the reconciliation rule distinguishes only “matching tombstone means update=1; otherwise update=0/refused” ([line 1064](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1064)). A successful eligible dry-run never creates a tombstone, so a crash before completion would be falsely finalized as an execute refusal. Define reconciliation separately for `dry_run=1`, using its persisted observation/proof, and test crashes between dry-run audit insertion and completion for eligible and ineligible results.

3. **HIGH — Permanent repair audits contradict the repository’s account-erasure contract.**  
   Design 180 makes `genesis_repair_audit` permanent with no cleanup path and stores account ID, operator/reason, and original claim bytes ([line 981](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:981)). Design 37 requires erasing every account-scoped D1 row and explicitly deletes forensic audit data; the actual hard purge deletes `account_keys` in [account-delete.ts](/home/via/Development/Personal/rbox-core/apps/api/src/account-delete.ts:352). The draft also says “invoke account deletion” as though claim deletion were immediate, whereas the route only schedules a later purge. Specify whether repair audits are erased, scrubbed into a minimal compliance record, or intentionally exempted with a revised privacy contract; place reconciliation at the actual `finishD1` boundary and test complete erasure/retention behavior.

4. **HIGH — Competing-genesis cleanup never specifies disposition of the losing `device.json` and `mk.key`.**  
   The abandoned-attempt quarantine manifest contains only `rk.key.staged` ([line 724](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:724)); “other staging artifacts” are merely said to be retired ([line 341](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:341)). If the journal is retired while the losing device/MK remain, current `hasDevice` routing can treat that machine as locally enrolled even though its device is absent from the winning roster. Enumerate receipt-authorized deletion or quarantine of both files, their crash-resume states, and tests proving no losing local enrollment survives journal retirement.

5. **MEDIUM — Workspace ownership is absent from the supposedly exhaustive client observation.**  
   `GenesisPresence` omits the authoritative workspace registry ([line 476](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:476)), although every repair-bootstrap statement guards on zero owned workspaces ([line 841](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:841)). Consequently, the document’s own anomalous workspace-bearing tombstone fixture appears `repair-ready` to the client, then repeatedly refuses server-side instead of becoming `integrity-failure`. Expose workspace ownership in the versioned observation or define another closed classifier signal, with client/state-machine tests.

6. **MEDIUM — Quarantine’s hardened-writer contract is not fully exercised by the required tests.**  
   The normative contract now correctly covers `quarantine-resume.json` and `completed.json` ([line 411](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:411)), but exhaustive injected temp-create/write/fsync/close, read-back, published-file-fsync, and parent-fsync failures are required only for the other genesis files ([line 1415](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1415)). Quarantine tests cover parser/state cases and coarse publication crashes, not each hardened-writer failure for both files and both quarantine users. Add those cases to prevent a call site from bypassing or failing to await part of the shared contract.

7. **MEDIUM — Workspace creation has no defined absent-claim behavior.**  
   The new batch requires a real, non-tombstone claim for both statements ([line 1002](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1002)), while today workspace creation does not require `account_keys` and proceeds in [authz.ts](/home/via/Development/Personal/rbox-core/apps/api/src/authz.ts:93). The draft defines 423 for tombstones but no result when both guarded statements affect zero rows because the claim is absent. Specify whether absent claims retain current behavior or return a new non-repair error, and test its result vector, response, and zero-side-effect behavior.

No files were edited.

WORK-COMPLETE