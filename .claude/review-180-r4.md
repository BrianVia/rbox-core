Verdict: **CHANGES-REQUIRED**

1. **BLOCKER — The 423 preflight is not linearizable with repair.**  
   The permit check and claim/presence observation are not required to use one snapshot in [design 180:383](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:383). A legal interleaving is: GET observes no permit; repair atomically inserts permit and deletes claim ([line 807](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:807)); GET then observes no claim and returns pristine. The old client overwrites `device.json`/`mk.key` at [e2ee-client.ts:110](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:110) and [e2ee-keystore.ts:93](/home/via/Development/Personal/rbox-core/src/cli/e2ee-keystore.ts:93) before POST returns 428. Tests at [design 180:1227](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1227) do not force this split-read race. Fold 1 remains open.

   Once a 423 is actually returned, every current GET caller propagates it: [remote/keys.ts:21](/home/via/Development/Personal/rbox-core/src/cli/remote/keys.ts:21), [auth-cmd.ts:128](/home/via/Development/Personal/rbox-core/src/cli/auth-cmd.ts:128), [setup-cmd.ts:522](/home/via/Development/Personal/rbox-core/src/cli/setup-cmd.ts:522), [e2ee-client.ts:153](/home/via/Development/Personal/rbox-core/src/cli/e2ee-client.ts:153), and [e2ee-remote.ts:894](/home/via/Development/Personal/rbox-core/src/cli/e2ee-remote.ts:894). The defect is endpoint observation atomicity, not caller propagation.

2. **BLOCKER — `ACTIVE` conflates fencing with completed witness authority.**  
   `ACTIVE` requires a permit linked to a “successful execute audit” at [design 180:782](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:782), while the transaction initially writes `outcome='attempted'` and completes it afterward ([lines 830–860](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:830)). Yet claim deletion inside the transaction already requires that permit to be ACTIVE ([line 823](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:823)). If completed success is required, normal deletion cannot qualify and a crash after the batch is unfenced. If `attempted` qualifies, the definition is ambiguous and an anomalous permit can cease fencing despite [line 838](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:838). Fence-active and witness-authorizing predicates must be separate. Tests do not exercise GET/POST/mutation/commit fencing while the audit is attempted or anomalous.

3. **BLOCKER — Permit expiry fails open instead of requiring manual escalation.**  
   Expiry makes the permit inactive at [design 180:782](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:782), and every GET, bootstrap, mutation, and commit fence checks only ACTIVE permits. After repair deleted the claim, expiry therefore makes old GET return 404; the old client generates replacement keys and POST is allowed. This contradicts invariant 9 and [lines 785–788](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:785), which promise manual escalation after expiry. Witness-disappearance tests exist, but no test proves the account remains fenced from incapable GET/POST after expiry.

4. **BLOCKER — A successfully retained repair quarantine immediately becomes illegal.**  
   `pristine` excludes quarantine state ([design 180:484](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:484)); the carve-out requires the same active witness ([lines 581–588](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:581)); bootstrap then consumes that permit ([line 872](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:872)); but the runbook and E2E test retain the archive ([lines 1092–1095](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1092), [1268–1273](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1268)). On the next classification, the retained manifest is stale-witness integrity failure. Fold 4 remains open; it needs a durable terminal/inert archive shape or manifest-retirement transition and post-consumption tests.

5. **HIGH — “Manifest first” omits directory creation.**  
   The manifest lives inside `quarantine/genesis-legacy-<auditId>/` ([design 180:546](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:546)), so that directory must be created before the claimed first mutation at [line 571](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:571). A crash after directory publication but before manifest publication leaves an empty extra artifact that is neither `repaired-legacy` nor `quarantine-resume`. Tests begin only after manifest publication at [line 1195](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1195).

6. **HIGH — Old setup/init can create authoritative workspace ownership without touching the 423-fenced endpoint.**  
   Old setup trusts local `device.json` at [setup-cmd.ts:190](/home/via/Development/Personal/rbox-core/src/cli/setup-cmd.ts:190) and can create a workspace at [line 826](/home/via/Development/Personal/rbox-core/src/cli/setup-cmd.ts:826). Init creates the workspace before E2EE account loading at [init-cmd.ts:387](/home/via/Development/Personal/rbox-core/src/cli/init-cmd.ts:387). The API inserts ownership without a claim/permit guard at [authz.ts:89](/home/via/Development/Personal/rbox-core/apps/api/src/authz.ts:89) and [routes/account.ts:37](/home/via/Development/Personal/rbox-core/apps/api/src/routes/account.ts:37). This can create the exact workspace-registry history that repair eligibility forbids, while only workspace commits—not creation—are fenced. Add the same repair fence to workspace creation and transition tests for old setup/init.

7. **HIGH — `competing-cleaned` says “quarantine” but defines no crash-resumable quarantine protocol.**  
   Both documents now consistently forbid promotion and bare unlink ([design 180:251](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:251), [design 179:212](/home/via/Development/Personal/rbox-core/docs/design/179-recovery-kit-macos-keychain.md:212)), closing the original behavior finding. But design 180 never defines the abandoned-attempt quarantine’s exact path, uniqueness rule, destination-byte validation, or source/destination legal states. After a crash following rename, `cleanup-resume` cannot unambiguously find and validate the losing RK. Tests merely assert that quarantine happened ([design 180:1176](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1176)); they do not inject each rename/publication seam.

8. **HIGH — Anomaly handling is not executable for every promised vector.**  
   The table says every non-normal vector records an anomaly with `auditId` ([design 180:835](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:835)), but when the first component is zero the conditional audit row does not exist. Only `0/0/0` gets a separate audit protocol ([line 843](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:843)), while tests demand every other vector at [line 1248](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:1248). Additionally, `1/0/0` has no permit, so the promised “preserve/fence state” is false. Either prove and restrict the possible vectors or define durable anomaly-audit and incident-fence protocols for them.

9. **HIGH — The specified 409 repair response collides with existing retry semantics.**  
   Design 180 requires `409 repair_in_progress`/`account_not_bootstrapped` at [lines 790–797](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:790). Current commit transport treats non-epoch 409 as parent conflict ([commits.ts:335](/home/via/Development/Personal/rbox-core/src/cli/remote/commits.ts:335)); the legacy path does likewise at [line 167](/home/via/Development/Personal/rbox-core/src/cli/remote/commits.ts:167); admit and roster also treat every 409 as retryable at [remote/keys.ts:33](/home/via/Development/Personal/rbox-core/src/cli/remote/keys.ts:33). Use 423 consistently for repair fencing or mandate discriminator-aware transport changes and tests.

10. **MEDIUM — The completion intent is not actually account/attempt-bound.**  
    It is called “strict, account-bound” at [design 180:260](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:260), but its schema at [lines 264–277](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:264) has no version, accountId, deviceId, attempt identity, or request digest. A stale/misplaced phrase or kit-path intent cannot be proven to belong to the active journal. Tests validate shape and target but omit identity/attempt mismatch. Cross-document ownership itself is aligned: design 180 owns the record; design 179 only consumes its selected target.

11. **MEDIUM — The historical old-client UX assertion is false.**  
    Design 180 says the old command fails with the server’s error message ([lines 395–400](/home/via/Development/Personal/rbox-core/docs/design/180-atomic-genesis-enrollment.md:395)). The shipped transport never reads a non-404 GET body ([remote/keys.ts:21](/home/via/Development/Personal/rbox-core/src/cli/remote/keys.ts:21)); [errors.ts:157](/home/via/Development/Personal/rbox-core/src/cli/remote/errors.ts:157) therefore emits only `keys/account failed: 423`, not `repair_in_progress`. Safety propagation works when 423 is returned, but the documented UX and test expectation are impossible.

Fold verification:

- R3 #1: **open** — non-atomic preflight race.
- R3 #2: behavioral rule folded in both documents; quarantine protocol remains incomplete.
- R3 #3: selection/reselection ordering folded; intent binding remains deficient.
- R3 #4: **open** — pre-manifest and terminal lifecycle seams.
- R3 #5: stale witness is rejected as required, but the surrounding attempted/expired fence lifecycle fails open.
- R3 #6: anomaly labels are folded; exhaustive audit/fencing mechanics are incomplete.
- R3 #7: closed; the new sync-route check is correctly described as future work.
- R3 #8: closed; “will own” language and CODEMAP requirement are correct.
- R3 #9: closed; [design 179:279](/home/via/Development/Personal/rbox-core/docs/design/179-recovery-kit-macos-keychain.md:279) accurately describes existing coverage and [lines 827–832](/home/via/Development/Personal/rbox-core/docs/design/179-recovery-kit-macos-keychain.md:827) add wrong/historical-envelope substitution.

All explicit file:line citations in both designs resolve to the claimed current code; I found no stale citation. The status numbers are internally assigned consistently—GET 423, incapable bootstrap 428, ordinary conflicts/admin refusals/mutation guards 409—but finding 9 shows the 409 choice is not compatible with existing client semantics.

WORK-COMPLETE