import {afterEach,beforeEach,expect,test} from "bun:test";import fs from "node:fs/promises";import os from "node:os";import path from "node:path";
import {accountGenesisLockPath,acquireAccountGenesisLock,acquireGenesisLockPair,genesisLockRoot} from "./genesis-locks.js";
const ACCOUNT="acct_0123456789abcdef";let home:string;beforeEach(async()=>{home=await fs.mkdtemp(path.join(os.tmpdir(),"rbox-genesis-locks-"));process.env.RBOX_HOME=home;});afterEach(async()=>{delete process.env.RBOX_HOME;await fs.rm(home,{recursive:true,force:true});});
test("account locks use a dedicated non-materializing namespace",async()=>{const lock=await acquireAccountGenesisLock(ACCOUNT);expect(accountGenesisLockPath(ACCOUNT).startsWith(genesisLockRoot()+path.sep)).toBe(true);await expect(fs.access(path.join(home,".rbox","e2ee",ACCOUNT))).rejects.toThrow();await lock.release();});
test("global then account pair holds both and validates grammar before naming",async()=>{const pair=await acquireGenesisLockPair(ACCOUNT);expect(await fs.readdir(genesisLockRoot())).toContain(`${ACCOUNT}.lock`);await pair.account.release();await pair.global.release();expect(()=>accountGenesisLockPath("acct_../escape")).toThrow(/malformed/);});
