NOT ALIGNED
1. HIGH, docs/design/314-attested-elision-audit.md:34, `attestSavedBase` does not prove the audit predicate: src/cli/sync/base-hash-attestation.ts:80-89 checks only hash identifiers, sequence, headers, gitRepos presence, and file count—not file values or gitRepos contents. A same-length corrupted read-back is attested and then evades design 269’s sole drift detector indefinitely. Minimal fix: retain one independent audit hash after every newly written file array, or add an exact store-produced content proof before seeding/skipping it.
2. HIGH, docs/design/314-attested-elision-audit.md:42, the design relies on “design 313,” but no such design or implementation exists in this tree; current global saves rematerialize rows, and no reviewed contract proves a future retained-rows-plus-delta projection equals both `packet.global.manifest` and durable rows. Minimal fix: land/cite 313 first with differential equality against `loadRawState`, plus foreign-CAS, reset, telemetry-writer, and concurrent-write invalidation tests.
3. MEDIUM, docs/design/314-attested-elision-audit.md:20, unchanged memo tokens prove freshness only when `loadState` probes them; they do not prove equality “at audit time” because another CAS may land afterward. The later elision CAS prevents an unsafe save, but the design’s stated proof is false. Minimal fix: state the actual guarantee—snapshot equality at load, followed by CAS-bound rejection/recomposition—and test that race.
4. MEDIUM, docs/design/314-attested-elision-audit.md:24, making root-level `sync-state-elision.ts` import `sync/base-hash-attestation.ts` reverses the dependency direction for state-plane adapters already importing the elision module. Minimal fix: after establishing a valid proof, expose a narrowly keyed audit-memo seed operation from `sync-state-elision.ts` and call it from push, reusing `auditHash`’s exact generatedAt/manifestSchema/meta key instead of teaching the audit about push attestation.