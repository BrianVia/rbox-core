# Root validation of deletion batching

Independent diff review confirms one working-map owner and batching restricted to consecutive native ENOENT/ENOTDIR observations. Literal slash-prefix semantics, input immutability and deferred behavior are covered by focused tests. Typed catch cleanup preserves the existing error classifier at both exception boundaries.

Independent benchmark rerun on supported Bun1.4.0: one warmup,33.235/32.980/30.928ms, median32.980ms for124,000 entries/1,000 consecutive absent directory events. Agent's initial candidate median26.95ms; historical same-fixture baseline1401.45ms. Thus observed wall-clock improvement spans42–52×; this is the absent-only workload, not an end-to-end sync speed claim.

DEV source-mode two-device `rig run type-flip` exited0, all9 assertions pass. Symlink obstruction preserved as a recoverable conflict copy; directory/file materialized and replicas converged. Throwaway account deletion200. Report: `scripts/rig/runs/20260905-083503-type-flip/report.md`. This source-mode run does not prove compiled-binary behavior and does not exercise every absent batching path. Focused tests and structural scan counts cover that algorithm.

DEV source-mode two-device `rig run mass-delete-guard` also exited 0: all 8 assertions pass. Receiver refused the unapproved mass deletion, preserved all 100 original files, then converged to an empty tree after the explicit allow flag. Throwaway account deletion returned 200. Report: `scripts/rig/runs/20260905-083611-mass-delete-guard/report.md`; bounded source logs: `/private/tmp/rbox-astra-type-flip.log` and `/private/tmp/rbox-astra-mass-delete.log`. These source-mode scenarios verify existing type-flip and deletion-safety behavior, not compiled artifacts or the entire watcher batching sequence.

Pre-PR F2 diff review rechecked every new flush boundary and the two narrowed filesystem catch sites against `c4aa22bbb`. No additional product defect found. The benchmark validates its expected survivor count and first surviving path, reports the runtime and warmup policy, and makes no CI wall-time or general sync-speed claim. Compiled-binary validation remains a separate root-owned gate.

After the parallel F1 fix landed in the working candidate, reran the six-file F2/ignore suite on Bun 1.4.0: **116 pass, 0 fail, 532 assertions** (`/private/tmp/rbox-290-pr-review-tests.log`). This closes the earlier unrelated tracked-index failure recorded in the historical 290 implementation notes. No source edits were needed in this final review.

F2 pre-push polish: inverse reconstruction of the pre-format manifest was verified by exact formatter roundtrip, then restored and given a one-line ownership header. Prior formatted-candidate SHA-256: `9a84cc23628cf310a0db39750c7afb6463e4bb44b0f4da385430651a9ccd7a08`. Final size is 755 nonblank lines / 35,863 bytes; the audited-cohesive manifest-only ratchet of 687 / 32,603 leaves no whole-line/byte headroom under existing 1.1 slack. Focused F2 tests: 68 pass; module-size suite: 6 pass. Affected lint has no F2 warnings (four parallel fingerprint warnings remain). No global size rule changed. Round-2 Claude Fable 5.1 medium cohesion review returned ALIGNED; see `docs/design/reviews/REVIEW-290-2.md` for existing-fixture confirmations and the correction that the parent-agent 32.98 ms rerun used the same workload. No further round was needed. Evidence: `/private/tmp/rbox-f2-polish-tests.log`, `/private/tmp/rbox-f2-polish-size.log`, `/private/tmp/rbox-f2-polish-lint.log`, and `/private/tmp/rbox-f2-restore-verify.mjs`.
