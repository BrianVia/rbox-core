NOT ALIGNED
1. HIGH, docs/design/313-delta-save-file-reuse.md:30, accepted staging does not prove object equality with read-back rows: codec/SQLite normalization can change values (for example, accepted `mtimeMs: -0` reads back as `0`), and the caller-owned array can mutate across the async CAS boundary. Minimal fix: derive the reused array from a memoized, token-matched store predecessor plus canonically decoded delta upserts; otherwise page the store.
2. HIGH, docs/design/313-delta-save-file-reuse.md:46, the drift claim is false: the production audit uses `loadState` and design-303’s array-keyed hash memo, not `loadRawState`; a divergent reused array can therefore be hashed as truth, cached, and never compared with file rows, while `finishProjection()` checks only tokens. Minimal fix: admit only store-derived/token-bound arrays and add a deliberately divergent-reuse test proving fallback or detection.
3. MEDIUM, docs/design/313-delta-save-file-reuse.md:3, direct packet reuse belongs neither in `cas-translation.ts` nor its adapter owner: it contradicts `cas-translation.ts:6` and `read-only.ts:85`, which prohibit trusting caller claims, and forks design 302’s memo-owned skip rule. Minimal fix: extend `state-memo.ts` with one accepted-delta reuse operation keyed by the retained predecessor’s lineage/nonce/revision/base generation and the accepted post-CAS token; canonicalize only delta upserts, apply them to retained rows, and return undefined for the existing paging fallback.
4. MEDIUM, docs/design/313-delta-save-file-reuse.md:57, the proposed tests cover only happy-path equality, full-global, and the kill switch; they do not establish the claimed “every accepted packet” contract across canonical normalization, mutation, retry/recomposition, expectation-carrying deltas, or rejected-delta fallback. Minimal fix: add those cases, including `mtimeMs: -0`, and assert both returned-state equality and durable-row equality.