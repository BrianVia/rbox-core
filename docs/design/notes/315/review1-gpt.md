NOT ALIGNED
1. BLOCKER, docs/design/315-attestation-keyed-by-files-array.md:21, `gitRepos(meta)` is a hashed reconstruction input but is neither keyed nor recorded. A new state wrapper can retain the attested files array while substituting `manifestMeta.gitRepos` and preserving `encManifestSha`/`manifestHash`; the proposed check then trusts a different reconstruction. This changes WHAT #816 trusts, not merely WHERE. Minimal fix: key by the immutable `lastSyncedManifest` and `manifestMeta` object identities—the elided projection preserves both—which solves the measured object-spread miss without widening trust; alternatively record and compare the complete `gitRepos` value and add the adversarial same-identifiers/different-gitRepos test.