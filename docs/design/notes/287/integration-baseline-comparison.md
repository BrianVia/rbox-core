# Bounded integration baseline comparison

Compared the candidate in `.claude/worktrees/astra-sync-git` (HEAD `ab84dfb30`, including the current working candidate) with an independent clean archive of **`c4aa22bbb81c8754230735a73c5a58a28a64b4f7`** at `/private/tmp/rbox-nongit-base-c4aa22b`. Extracted `src`, `scripts`, `package.json`, `tsconfig.json`, and `bunfig.toml` directly with `git archive`; shared installed dependencies through a node_modules symlink. This is not the separate adoption archive, which had subsequently been modified for another probe. No source/index changes were made for this comparison.

Both executions used **Bun 1.4.0, macOS arm64, `TMPDIR=/private/tmp`**, and approved execution with process inspection access. Same nine files and test-name filter: candidate **30 pass / 11 fail / 197 filtered / 130 assertions**, baseline **30 pass / 11 fail / 197 filtered / 130 assertions**. The named failure sets are identical. Logs: `/private/tmp/rbox-nongit-candidate.log` and `/private/tmp/rbox-nongit-baseline.log`.

| Identical baseline/candidate failures | Evidence and classification |
|---|---|
| Design 237: `replacement closes old watcher/ref/debouncer and fences every late callback`; `stop during Git-ref attachment cannot reinstall the drained candidate` | Baseline platform-dependent fixture assumptions. `watcher-session-supervisor.ts` uses `gitRefSideChannelEligible(process.platform, backend)`; `git-ref-watch.ts` deliberately permits only Linux + Parcel. On macOS the fixture's attach callback never executes, yielding missing `attach-ref` and undefined `release`. |
| `163 v13 negative control: a read-only open of the inspected database fails that snapshot` | Baseline Bun/SQLite/platform behavior: raw readonly query raises `SQLITE_CANTOPEN` before the test can inspect its expected sidecars. No candidate-specific regression demonstrated. |
| `frozen repo_records columns stay bijective with the nineteen live RepoRecord members` | Same `SQLITE_CANTOPEN` on the first raw readonly `PRAGMA table_info`; this result does **not** demonstrate a schema/member mismatch. |
| `a refused foreign WAL database is left byte-for-byte untouched` | Same fixture-precondition failure: three files remain after its checkpoint/close, where the test expects one, before product refusal is exercised. Baseline runtime behavior. |
| `containment initialization never leaks a SQLite handle` | Same missing `/proc/self/fd` on macOS. This is a Linux-only FD-count fixture assumption, not evidence of a SQLite leak. |
| 207.1 `repo removal prunes empty skeletons while preserving .git and repo quarantine`; 207.2 `repo removal leaves a nested repository and its ENOTEMPTY ancestor intact`; 207.5 `an rmdir failure leaves only that subtree and never surfaces from apply`; 207.9 `cloned repo removal loses its empty skeleton and doctor names the retained repository`; `the anchored sweep leaves a retained .git byte-exact` | Same five empty-directory survival assertions fail on baseline and candidate. Existing sweep uses pinned-directory paths (`/proc/self/fd` on Linux, `/dev/fd` otherwise) and catches cleanup failures. Likely related to the already-observed macOS anchored-filesystem behavior, but this comparison establishes baseline equivalence rather than claiming a fully isolated native root cause. |

`keyed setup persists credentials with private file mode` **passes in both focused executions**. The broad suite's missing expected credential file is therefore order/environment-dependent and remains **unattributed**, not proved fixed or a demonstrated candidate regression. The fixture changes HOME only; product credential routing gives `RBOX_HOME` precedence, making leaked environment a plausible hypothesis, not an established cause.

The broad log's **two unhandled errors are Git-follow errors**, not SQLite errors: a command targeting a temporary repo already removed after a timed-out test, and `P settlement artifact mismatch for repo:refs/heads/many-26` around the other timed-out follow test. Their regression classification belongs to the Git-follow comparison owner. This note does not clear them.

Reproduction: run `bun test` with the nine files `setup-cmd.test.ts`, `state-plane/authority-bootstrap.test.ts`, `daemon/watcher-session-supervisor.test.ts`, `sync-git/lazy-apply-probes.test.ts`, `sync-git/follow-matrix.test.ts`, `sync-git/remote-repository-deletion.contract.test.ts`, `state-plane/schema/inventory.test.ts`, `state-plane/schema/store-open.test.ts`, and `state-plane/store/lifecycle-and-containment.test.ts` under `src/cli/`, with `-t 'design 237|163 v13 negative|frozen repo_records|foreign WAL|containment initialization|keyed setup persists|207\.|anchored sweep'`, separately from each root.

This bounded result establishes no new regression in the eleven reproduced non-Git failures. It does not make the 4,539-test affected suite green, excuse new failures, or replace Linux/compiled CI and the separate adoption/Git/module-size reviews. No source/index edit, production mutation, or merge was performed here.
