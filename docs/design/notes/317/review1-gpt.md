NOT ALIGNED
1. HIGH, docs/design/317-parallel-git-artifact-flush.md:25, the design specifies detached siblings, but `planGitSections` immediately calls `dispose()` on rejection, deleting ciphertext still being read; it also falsely says retained files survive into the next push. Minimal fix: specify the implemented latch-and-drain semantics and next-push recapture.
2. HIGH, src/cli/sync-git/plan-artifacts.ts:87, `undefined` is both the no-failure sentinel and a valid rejected/thrown value; `throw undefined` is silently treated as success, violating the flush barrier. Minimal fix: track failure with a separate boolean or unique sentinel and test rejection with `undefined`.
3. MEDIUM, docs/design/317-parallel-git-artifact-flush.md:30, concurrency 4 does not match the blob uploader’s current bounds (64/512 caller concurrency, 24 batch slots, 16 pack streams), and no sweep establishes 4 as the server-cost knee; each artifact still issues its own account-scoped `has` request. Minimal fix: correct the rationale and benchmark 1/2/4/8 with request counts and server timings before declaring 4 authoritative.