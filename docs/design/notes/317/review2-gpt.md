NOT ALIGNED
1. MEDIUM, docs/design/317-parallel-git-artifact-flush.md:37, round-1 finding 3 remains unresolved: concurrency 4 is unmeasured, and the claimed ≤6 artifacts is false—capture emits one artifact per op-state file, potentially many. Four callers also cannot reach the existing pack uploader’s 16-record activation threshold, causing repeated fallback-delay waves, while every artifact still incurs an account-scoped `has`. Minimal fix: correct the artifact-count rationale and benchmark 1/2/4/8/16 with request counts, `fn`, uploader dispatches, and server accounting timings before fixing the bound at 4.