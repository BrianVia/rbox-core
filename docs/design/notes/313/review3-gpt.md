NOT ALIGNED
1. HIGH, src/cli/state-plane/store/plane-promotion.ts:27, interning can select an existing row with matching semantic columns but corrupted `canonical_bytes` or `retained_estimate`; normal read-back detects this through `decodeFileEntry`, while synthesized reuse skips the cursor, returns successfully, and memoizes the divergence. Minimal fix: include both derived columns in `EXACT_MATCH` and test reuse against a corrupted pre-existing intern candidate.
2. HIGH, docs/design/313-delta-save-file-reuse.md:63, token matching does not prove the retained files came from store rows: `whole-state-compat.ts:269` also retains fully-elided `acceptedProjection` values, which are caller-supplied. A projected predecessor can therefore satisfy every proposed predicate while its files differ from the store. Minimal fix: prevent projected results from seeding delta reuse unless a matching store-derived memo entry already existed, otherwise page.
3. MEDIUM, docs/design/313-delta-save-file-reuse.md:32, the guard omits `authorityId` even though it is already part of `StateFreshnessToken`; an authority-only replacement can preserve every listed field while changing rows. Minimal fix: require equal `authorityId` and add an authority-only replacement fixture with different files.
4. MEDIUM, docs/design/313-delta-save-file-reuse.md:75, expectation-carrying deltas do exist at the accepted adapter contract: `applyStateSavePacket` accepts hand-built `StateSavePacket`s and store admission does not reject `globalDelta` plus `elisionExpectation`. Composer exclusivity proves only one caller path. Minimal fix: either reject that combination at admission or test an accepted matching-expectation delta and its rejected/recomputed counterpart.
5. MEDIUM, docs/design/313-delta-save-file-reuse.md:64, round-1’s undetectable-divergence finding remains unresolved: the admitted same-token backup-restore case returns memoized rows, and the normal drift audit uses `loadState` plus the array-keyed hash memo, so it never compares them with the restored rows. Minimal fix: add authenticated base-content evidence to the reuse predicate, or explicitly narrow the guarantee and obtain product approval for this safety regression.