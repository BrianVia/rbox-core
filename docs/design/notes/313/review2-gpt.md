NOT ALIGNED
1. HIGH, docs/design/313-delta-save-file-reuse.md:40, the proposed codec round-trip does not reproduce staging: `encodeFileEntryForConsume` retains the input’s `mtimeMs: -0`, while the sealed canonical JSON and SQLite row become `0`; the reused entry can therefore differ from the cursor result and then be memoized, hiding drift. Minimal fix: normalize from the canonical staged value—parse `encoded.canonical`, re-encode, then decode the row—and compare against an actual store cursor.
2. HIGH, docs/design/313-delta-save-file-reuse.md:54, “the packet is the composer’s frozen value” is false: `StateSavePacket` and its delta are mutable, and `saveThroughStore` rereads `packet.globalDelta` after awaiting the CAS. Mutation after staging can make memo derivation use different ops than the store committed, and the resulting retained state prevents the drift audit from discovering it. Minimal fix: create one detached, canonically normalized delta before the CAS, pass that exact private value to both staging and `memoizedDeltaFiles`, or return authenticated committed-delta evidence from the store boundary.
3. MEDIUM, docs/design/313-delta-save-file-reuse.md:68, round-1’s adversarial coverage remains incomplete: there is no accepted expectation-carrying delta case, and the line-77 store mutation makes the store reject before `memoizedDeltaFiles` runs, so it does not prove divergent retained rows trigger paging. Minimal fix: add an accepted delta with a matching `elisionExpectation`, plus a direct memo test where retained rows cannot satisfy the delta and assert `undefined`; separately test rejection/recomposition fallback and fresh-row equality.