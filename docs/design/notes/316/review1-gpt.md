NOT ALIGNED
1. BLOCKER, docs/design/316-marks-over-cap-stays-on-delta.md:46, `phase1Purge` uses an earlier DO-roots snapshot and deletes eligible refs solely against that snapshot (`apps/api/src/gc-phase1.ts:139-160`); the active-intent probe does not make purge see a subsequently published head. Marker regrant is the D1-serialized barrier, while grace is explicitly documented as secondary, so skipping regrant lacks a correctness proof. Minimal fix: retain the existing enforce fallback until purge/publication share a real fence, or formally bound snapshot-to-delete below grace and add the exact stale-snapshot/publish/purge interleaving test.
2. HIGH, docs/design/204-delta-scoped-publish.md:62, the later shipped design explicitly treats `markedProbeSkipped` full admission as a safety mechanism and hard flip precondition; its regression test pins that behavior at `apps/api/test/commit-delta-shadow.test.ts:207`. Git blame shows the fallback was deliberate in `1cf847d7f` (“ENFORCE safety”), not an accidental divergence from Q3. Minimal fix: preserve the fallback and revise design 316/Q3, or supersede design 204 only after proving and testing a replacement fence.
3. MEDIUM, docs/design/316-marks-over-cap-stays-on-delta.md:43, this is not a metric rename: `marks_over_cap` is already emitted, while the proposal removes `fallback{reason=marks_over_cap}`. The repository soak endpoint will continue working but its fallback totals/by-reason semantics change (`apps/api/src/admin.ts:433-465`), and the existing endpoint test expects both points (`apps/api/test/commit-delta-shadow.test.ts:227-228`); no external-dashboard compatibility evidence is provided. Minimal fix: describe it as retirement of the fallback event, update the pinned test only with the safety change, and verify external dashboard/alert consumers before rollout.