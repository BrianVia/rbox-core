# Perf cycle #661 + #477 — "before" baseline (2026-08-17 ~23:40Z, fleet on 2.0.0-beta.4-dev+505738e)

Captured from live daemon logs before any change. Perf-differential close-out
compares against these exact shapes.

## Desktop (via-desktop-ubuntu, 107 repos, ~127k files) — zero-change push

Five consecutive samples: **7.2 / 7.4 / 7.5 / 7.6 / 7.7 s**. Stable composition:

| leg | cost |
|---|---|
| gap start→state-load | 1.9–2.3s (unnamed) |
| state-load | 1.8–1.9s |
| gap state-load→git-plan | 0.5–0.6s |
| git-plan | 0.6–0.7s (d≈490ms discover dominates; projection 0.5s inside) |
| gap git-plan→end (contains publish_transition 1.8–2.2s) | 2.2–2.6s |

Content push sample (23:41:59, sp1): 17.0s — commit 4.7s (p1.7 e1.7),
state-save 2.1s, state-save→end gap 2.5s, delta_base 1.1s.
drain_wait ≈ 0 everywhere (the #690 fix holds).

## Mac (dfinitiv-macbook-pro, 104 repos) — zero-change push

Three samples: **6.6 / 6.2 / 7.0 s**:

| leg | cost |
|---|---|
| gap start→state-load | 1.2s |
| state-load | 1.1–1.2s |
| git-plan | 2.4–2.8s (d 0.8–1.3s, cy 0.45–0.61s, f 0.48–0.6s, cp up to 0.77s, h 0.08–0.13s) |
| gap git-plan→end (contains publish_transition 1.2s) | 1.3–1.5s |

matcher=0.0 / state_lineage=0.0 on both hosts — earlier Mac matcher findings
(1.7s construction) not present in these samples; re-check under load.

## flat-meadow — PULL-ONLY again (confirmed: "periodic-scan mode; no live
watch", every pull `local=scan skip=p1-watcher`) — zero-change pull

Three samples: **51.1 / 51.6 / 63.2 s** (files=127,086; repos=107 with
unchanged=55, deferred=9, skippedHeld=43):

| leg | cost |
|---|---|
| state-load | 1.7–1.8s |
| scan (p1-watcher skip → full local scan) | 10.3 / 12.0 / 17.2s |
| git-apply | 22.0 / 22.8 / 27.7s — queueMs p95 ≈ 22–28s |
| → dominated by repo i25: fetchDecryptMs 12.5–17.3s EVERY pull | zero-change refetch |
| cas acquire (lock ceremony, locks=1645) | 11.0–11.8s |
| state-save | 2.5–3.5s |

Defect-smell beyond #477: a deferred repo (i25) fetches+decrypts a large
bundle on every zero-change pull — no cache/skip for a repo that will be
deferred anyway. Also repos i1/i3/i15/i21 pay 1–5s fetchDecrypt each pull.
The 11–12s cas acquire at N=1645 locks is the #749 residual.

## e2e context

Overnight observed propagation: 41–46s desktop→Mac, ~51s desktop→FM.
Founder target: ≤10s e2e. Sender push op target 4–5s (#661 round-6 ruling).
