1. CRITICAL — r1 finding 1 remains contradicted inside the design. §1 still calls the sender delay root-caused by macOS lacking a Git-ref channel and `.git` being pruned ([design:18](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/docs/design/232-reactive-propagation.md:18)), while §4 correctly retracts both claims ([design:62](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/docs/design/232-reactive-propagation.md:62)). The sender row must say “~60s measured; cause unattributed pending trace.” Likewise, “safety scan stops being the primary commit detector on any platform” ([design:48](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/docs/design/232-reactive-propagation.md:48)) is an unimplemented outcome, not preserved behavior; Chokidar remains intentionally scan-bound for Git ([watcher.ts:431](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/watcher.ts:431)).

2. MAJOR — untouched-frame sequence correlation is feasible, but the promised hop table is not exact yet. The POST response and existing WS frame carry the same sequence, so no frame change is required ([commits.ts:376](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/remote/commits.ts:376), [workspace-sync.ts:757](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/apps/api/src/workspace-sync.ts:757)). However:

   - Broadcast precedes both the D1 mirror and HTTP response, so response time is not server emit time ([workspace-sync.ts:761](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/apps/api/src/workspace-sync.ts:761)). `emit→receive` therefore needs separately correlated server telemetry or must be renamed.
   - Live-fleet cross-host times require an explicit clock-skew bound.
   - Durable `onPullApplied` carries only actions, not adopted sequence ([pull.ts:464](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/sync/pull.ts:464), [deps.ts:127](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/sync/deps.ts:127)). Coalesced frames/pulls can therefore misjoin “apply complete” unless completion records the adopted sequence or verifies both target state and a unique content/HEAD witness.

3. MAJOR — the bench/sender-trace slice duplicates ownership and does not specify the needed observation seam. The existing [git-commit-propagation.ts](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/scripts/rig/scenarios/git-commit-propagation.ts:1) already owns two-host orchestration, ordinary-write versus ref-only-commit stimuli, event-driven sender proof, and notify-carried receiver proof. Extend/extract that harness and reuse [propagation-report.ts](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/scripts/propagation-report.ts:34), rather than add a second authority. The design must also require both a unique ordinary write and a pre-armed ref-only commit; a marker write alone cannot investigate the Git-ref failure.

   Sender trace needs explicit local-only hook points and correlation: backend armed, native event/classification, debouncer push/fire, first false→true scheduler arm, operation begin, and publish receipt sequence. `onRawEvent` currently excludes Git signals, which bypass the file batcher ([watcher.ts:225](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/watcher.ts:225), [watcher.ts:346](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/watcher.ts:346)).

4. MAJOR — the residual-bucket slice cannot meet its own 5% closure gate as written. The named buckets omit directory `lstat`/dircache work, directory-entry conversion, symlink reads/hashing, callbacks, recursion/control, and finalization ([manifest.ts:521](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/engine/manifest.ts:521)). More importantly, a failed pruned attempt’s `winningStats` is discarded while wall time includes both attempts ([manifest.ts:152](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/engine/manifest.ts:152)). Specify exhaustive, mutually exclusive, all-attempt accounting; fixed path-free fields; one monotonic clock for wall and buckets; an absolute epsilon/minimum sample size; and incremental enabled/disabled A/B overhead methodology. Preserve `Date.now()` for semantic dircache timestamps.

5. MAJOR — fused/suspect visibility cannot use the existing `watcher-degraded` reason alone. A stable scan clears `watcherDegraded` before checking whether trust actually recovered, so suspect or permanently fused trust can become ambient `synced` ([daemon.ts:2290](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/daemon.ts:2290)). Backend-coverage downgrade also fuses `trustState` without setting that boolean ([daemon.ts:2951](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/daemon.ts:2951)). Add an independent optional ambient field derived directly from trust state, retaining `watcher-degraded` for generic backend degradation.

   Full `rbox status` also currently discards ambient attention/trust: observation retains `trustedAmbient`, but status projects only daemon version and mode ([observation.ts:116](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/observation.ts:116), [status-projection.ts:158](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/status-projection.ts:158)). The design must specify ambient validation, fresh-incarnation admission, status projection, brief/verbose/JSON rendering, precedence with halt/quota, compatibility with old records, and unchanged health/readiness/exit semantics.

6. MAJOR — r1 disposition ledger:

   - Finding 1: not resolved because the falsified §1 diagnosis remains.
   - Finding 2: mechanism correctly gated telemetry-only in §4.3, but §1 still causally calls drops self-inflicted and §3 says attribution “changes” ([design:21](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/docs/design/232-reactive-propagation.md:21), [design:51](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/docs/design/232-reactive-propagation.md:51)). Rewrite these as observed overlap/desirable outcome, not established causation or trust policy.
   - Findings 3–5: correctly evidence-gated with the missing delta authority, scheduler urgency/scan bound, and partial-observation receipt named.
   - Finding 6: wire-shape contradiction resolved, and deferral behavior correctly moved to design 234; exact bench landability remains unresolved by findings 2–3 above.
   - Finding 7: attempted but not resolved; finding 4 above blocks the bucket contract.
   - Finding 8: still partly unresolved—§1 continues to say settled file events call `requestPush`, while the callback actually calls `request("push")` ([daemon.ts:852](/home/via/Development/Personal/rbox-core/.claude/worktrees/232-reactive-propagation/src/cli/daemon/daemon.ts:852)).

Validation: 192 targeted watcher, WS, trust, ambient-status, scan, and propagation tests passed with 0 failures. This confirms current behavior; it does not close the specification gaps above.

**NOT-ALIGNED**