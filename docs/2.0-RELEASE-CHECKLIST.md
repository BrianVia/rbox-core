# 2.0 release checklist

Living gate list for shipping v2.0.0 stable. Every unchecked box blocks the
tag unless the founder explicitly waives it. Tag + production promotion each
need their own fresh explicit yes regardless.

## Packaging & distribution

- [x] **Bundle RboxBar with the release** — MECHANISM SHIPPED (#687, design
      245): every `v*` tag publishes a universal, tag-versioned
      `releases/v<version>/RboxBar-<version>.zip` (stable alias
      `releases/RboxBar.zip`), fail-closed with the release. First real
      exercise = next tag. Remaining founder decisions:
  - [ ] Developer ID signing + notarization (ad-hoc today → external users
        must right-click-open; design 179A Keychain work needs the signed app).
  - [ ] `rbox upgrade` app management (install/update/restart RboxBar).
  - [ ] install.sh / site mention of the download.
- [ ] #668: `rbox.to/next/install.sh` serves the marketing SPA instead of the
      installer — the beta onboarding door is broken.
- [ ] #649 GH Releases automation: PR open; every tag should get a Releases
      entry without manual backfill.
- [ ] Changelog: 2.0 heading complete, honest breaking-changes section.

## Product decisions due at tag time

- [ ] #667: does fossil disposal (design 236) ship to external users?
- [ ] Client-skew story for the 4 external users (Max, Ryan + 2): what happens
      to a 1.11.4 client against 2.0 server state, and the upgrade note that
      goes out (non-developer copy bar applies).
- [ ] Breaking-changes posture: 2.0 is pre-agreed breaking (no legacy
      migrations, 3 external users manually fixable) — DRAFTED in
      `docs/2.0-UPGRADE-NOTE.md` (founder review pending; §A7 = open
      questions). Wire verified UNCHANGED at both tags (schema 4, no version
      gates) — staggered fleets are safe; every skew problem is machine-local.
- [ ] **#688 BLOCKER-CLASS: `rbox upgrade` 1.11.4→2.0 leaves sync DOWN** —
      one-way SQLite conversion happens, then daemon restart hits the 231
      admission gate ("config absent; run rbox config regenerate") and exits 1.
      Decide: upgrade runs generation itself / refuses up front / installer-
      first recipe. Fix-or-decide before the stable tag.
- [ ] Next-channel stranding: users on `next` sit on beta.2 forever after
      stable v2.0.0 tags (channel persists; doctor shows a misleading update
      hint). Needs a channel-fallthrough or a release-note instruction.

## Quality gates (hard)

- [ ] #658 git-join-ahead rig regression fixed (PASS Jul 31 → FAIL Aug 13);
      bisect recipe in task #25.
- [ ] Full rig FAST suite green on the tag candidate (founder rule: rig before
      any tag).
- [ ] `bun scripts/ux/regress.ts` all-PASS on the tag candidate.
- [ ] #664 Mac field acceptance: 5 boots / 0 fuses AND RSS < 8GB over 24h.
      RSS hit 22.7GB on 2026-08-13 (partly the #683 loop — re-measure after
      that fix ships; if it still climbs, memory investigation becomes a
      blocker of its own).
- [ ] #683/#685 echo-publish + conflict-retry fix shipped and field-verified
      (desktop no-op cadence ~0, Mac drains pending, deferral list stays sane).
- [ ] b2 follow-up: un-ACKed config authorship re-publish fixed (the last
      empty-sequence source; field log line from #685 names it first).

## Known sharp edges to triage (ship-with-note vs fix)

- [ ] #672 `rbox stop` SIGKILL at 60s can orphan witness locks mid-apply.
- [ ] #669 sync-echo on git-behind checkouts (file-sync delivers, git pull
      refuses).
- [ ] #659 remainder: untransportable-path equivalence / stale-extra
      self-heal; Personal/blog commit-graph deferral (last deferred repo).
- [ ] #535 echo-clobber (.claude/worktrees carve-out is the standing
      workaround).
- [ ] cli-audit batch (#513-#521): pick the ones that embarrass 2.0 (silent
      wrong-answer ones: #515, #516, #518) and fix or accept explicitly.

## Perf targets (founder yardstick: ≤10s e2e propagation)

- [ ] Sender push: named costs are discover+capture (~0.9s desktop) + Mac
      3.4s state-load→git-plan gap (#661); decide which slices ship in 2.0
      vs after.
- [ ] Re-run the 30-attempt propagation bench on the tag candidate and record
      the numbers in STATUS.

## Release mechanics (existing, don't skip)

- [ ] Version bump both sites (package.json + CHECKED_IN_RBOX_VERSION).
- [ ] Tag only the exact SHA with green main CI; stable tag needs the
      changelog heading first.
- [ ] Fleet upgrade + `rbox stop && rbox start` per host after publish.
- [ ] Fresh founder yes for: the tag, and separately any production promotion.
