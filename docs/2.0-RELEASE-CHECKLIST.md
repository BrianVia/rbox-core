# 2.0 release checklist

Living gate list for shipping v2.0.0 stable. Every unchecked box blocks the
tag unless the founder explicitly waives it. Tag + production promotion each
need their own fresh explicit yes regardless.

Refreshed 2026-08-19 against live issue state; history for checked items is
in STATUS.md.

## Packaging & distribution

- [x] **Bundle RboxBar with the release** (#687, design 245) — every `v*`
      tag publishes tag-versioned `releases/v<version>/RboxBar-<version>.zip`
      (stable alias `releases/RboxBar.zip`), fail-closed with the release.
      First real exercise = next tag.
  - [x] Developer ID signing + notarization — DEFERRED (founder, 2026-08-14:
        no Apple Developer account yet). Ad-hoc stays; external users
        right-click-open. Revisit with the 179A Keychain work.
  - [ ] `rbox upgrade` app management (install/update/restart RboxBar).
  - [ ] install.sh / site mention of the download.
- [x] #668 `rbox.to/next/install.sh` beta onboarding door — FIXED (closed).
- [x] #649 GH Releases automation — MERGED; every tag gets a Releases entry.
- [ ] Changelog: 2.0 heading complete, honest breaking-changes section.

## Product decisions due at tag time

- [ ] #667: does fossil disposal (design 236) ship to external users?
- [x] Client-skew + migration posture — **DECIDED (founder, 2026-08-13):
      every external user starts FRESH on 2.0.** No upgrade note, no in-place
      migration.
- [x] #688 1.x→2.0 upgrade bricking — FIXED (PR #774, design 276): admission
      is checked per-workspace BEFORE the stop; upgrades no longer take sync
      down; real remedy surfaced. Field-proven on FM.
- [ ] **Wire-rename cutover** (`docs/wire-rename-candidates.md`): the v2-beta
      breaking window CLOSES when 2.0 ships externally. Every rename still
      wanted must land before the tag or wait for 3.0. Founder pass over the
      ledger required.
- [ ] Next-channel stranding: users on `next` sit on the last beta forever
      after stable v2.0.0 tags (channel persists; doctor shows a misleading
      update hint). Fresh starts sidestep it for external users; founder
      fleet switches with `rbox upgrade --channel latest`. Accept or fix.

## Quality gates (hard)

- [x] #658 git-join-ahead rig regression — FIXED (closed).
- [x] #683/#685 echo-publish ring + conflict-retry burn — SHIPPED (#685
      merged) and field-verified in the 277 soak (desktop no-op cadence ~0).
- [ ] Full rig FAST suite green on the tag candidate (founder rule: rig
      before any tag).
- [ ] `bun scripts/ux/regress.ts` all-PASS on the tag candidate.
- [ ] #664 Mac field acceptance: 5 boots / 0 fuses AND RSS < 8GB over 24h.
      Re-measure now that #683/#685 shipped; if RSS still climbs, memory
      investigation becomes a blocker of its own. STILL OPEN — the one gate
      that can hide a real embarrassment.
- [ ] b2 follow-up: un-ACKed config authorship re-publish (the last
      empty-sequence source; field log line from #685 names it first).
      Status unverified since #685 merged — needs a field-log check; close
      or convict.
- [ ] #660 CI shard cross-file state leak — flaky-CI net for the tag
      candidate; fix or accept with the flake registry entry.

## Known sharp edges to triage (ship-with-note vs fix)

- [ ] #672 `rbox stop` SIGKILL at 60s can orphan witness locks mid-apply.
- [ ] #669 sync-echo on git-behind checkouts (file-sync delivers, git pull
      refuses).
- [ ] #659 remainder: untransportable-path equivalence / stale-extra
      self-heal; receiver-local extras (incl. rbox's own .conflict copies)
      wedge the oracle.
- [ ] #535 echo-clobber (.claude/worktrees carve-out is the standing
      workaround).
- [ ] #775 unreadable-journal repos (pegasus, savvy-core) re-defer forever;
      #781 typed escalation for >24h-broken repos (doctor never escalates
      past info today — known, deliberate). External users WILL hit held
      repos; the doctor/status story for them is part of shipping safely.
- [ ] #702 structurally wedged repo: >512-key git config can never
      supersede — carries pending forever.
- [ ] cli-audit batch (#513-#521): the silent wrong-answer ones (#515,
      #516, #518) fix or accept explicitly before external users.

## Perf targets (founder yardstick: ≤10s e2e propagation)

- [x] Sender push — MET (#661 closed at target: desktop 1.5s median, Mac
      2.7s, was 7.4/6.6). 277 soaked numbers in STATUS.
- [ ] desktop→FM pull lane: 20.5-22s today; design 279 (delta-scoped CAS
      locks, in flight) projects ~9-10s. Land + field-confirm before tag,
      or record the miss as a named trade.
- [ ] Re-run the 30-attempt propagation bench on the tag candidate and
      record the numbers in STATUS.

## Release mechanics (existing, don't skip)

- [ ] Version bump both sites (package.json + CHECKED_IN_RBOX_VERSION).
- [ ] Tag only the exact SHA with green main CI; stable tag needs the
      changelog heading first.
- [ ] Fleet upgrade + `rbox stop && rbox start` per host after publish.
- [ ] Fresh founder yes for: the tag, and separately any production
      promotion.
