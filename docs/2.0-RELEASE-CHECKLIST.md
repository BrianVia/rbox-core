# 2.0 release checklist

Living gate list for shipping v2.0.0 stable. Every unchecked box blocks the
tag unless the founder explicitly waives it. Tag + production promotion each
need their own fresh explicit yes regardless.

Refreshed 2026-08-19 against live issue state; history for checked items is
in STATUS.md.

## Packaging & distribution

- [x] **Bundle RboxBar with the release** (#687, design 245) — every `v*`
      tag publishes tag-versioned `releases/v<version>/RboxBar-<version>.zip`
      (stable alias `releases/RboxBar.zip`), fail-closed with the release.
      First real exercise = next tag.
  - [x] Developer ID signing + notarization — DEFERRED (founder, 2026-08-14:
        no Apple Developer account yet). Ad-hoc stays; external users
        right-click-open. Revisit with the 179A Keychain work.
  - [ ] `rbox upgrade` app management — **DECIDED (founder, 2026-08-20):
        default ON with RBOX_NO_MENUBAR_APP=1 opt-out.** Implement per the
        draft design (needs the release-manifest artifacts key + API route
        fix; CLI install path is quarantine-free).
  - [ ] install.sh / site — **DECIDED (founder, 2026-08-20): install.sh
        stays a binary swap and prints one darwin-only pointer line; NO
        browser download link until Developer ID signing (179A).**
- [x] #668 `rbox.to/next/install.sh` beta onboarding door — FIXED (closed).
- [x] #649 GH Releases automation — MERGED; every tag gets a Releases entry.
- [ ] Changelog: 2.0 heading complete, honest breaking-changes section.

## Product decisions due at tag time

- [x] #667: fossil disposal — **DECIDED (founder, 2026-08-20): ships ON to
      everyone in 2.0.** Kill switch remains.
- [x] Client-skew + migration posture — **DECIDED (founder, 2026-08-13):
      every external user starts FRESH on 2.0.** No upgrade note, no in-place
      migration.
- [x] #688 1.x→2.0 upgrade bricking — FIXED (PR #774, design 276): admission
      is checked per-workspace BEFORE the stop; upgrades no longer take sync
      down; real remedy surfaced. Field-proven on FM.
- [x] Upgrade-path discrepancy — **RESOLVED (founder, 2026-08-20): healing
      IS the supported 1.x→2.0 path.** The Aug-13 "start FRESH" record is
      superseded by what shipped (#774 per-workspace admission + healing,
      field-proven). Changelog written to match; no refusal built.
- [ ] **Wire-rename cutover — DECIDED (founder, 2026-08-20): FULL pass, all
      12 candidates land before the tag** (dual-accept API windows +
      stored-data handling where needed). Work item, now on the critical
      path; box closes when the ledger is empty.
- [x] Next-channel stranding — **FIXED (this PR, founder ruling 2026-08-20).**
      When stable is newer than next's latest, `rbox upgrade` follows stable
      and clears the persisted selection.

## Quality gates (hard)

- [x] #658 git-join-ahead rig regression — FIXED (closed).
- [x] #683/#685 echo-publish ring + conflict-retry burn — SHIPPED (#685
      merged) and field-verified in the 277 soak (desktop no-op cadence ~0).
- [ ] Full rig FAST suite green on the tag candidate (founder rule: rig
      before any tag).
- [ ] `bun scripts/ux/regress.ts` all-PASS on the tag candidate.
- [ ] #664 Mac field acceptance — **bar REDEFINED (founder, 2026-08-20):
      phys_footprint < 2GB over 24h AND every fuse re-arms within 5 minutes**
      (fuses may fire; staying fused fails). Replaces the literal
      5-boots/0-fuses/8GB-RSS bar (RSS overcounts ~10x on darwin; fuses
      self-heal in ~2min). Run the 24h measurement against the new bar and
      close.
- [x] b2 follow-up: un-ACKed config authorship re-publish — **CLOSED with
      field evidence (2026-08-20)**: zero instances across all surviving
      fleet logs; detector string proven in deployed binaries + unit test.
      BUT "last empty-sequence source" was falsified: ~40 empty ops=0
      publishes/48h from post-pull re-arms on permanent-carry repos —
      filed as **#793** (desktop's carry is #702's field form). Triage
      #793 as ship-with-note vs fix.
- [ ] #660 CI shard cross-file state leak — flaky-CI net for the tag
      candidate; fix or accept with the flake registry entry.

## Known sharp edges — TRIAGED (founder, 2026-08-20)

**FIX before tag:** #535 echo-clobber, #515 swallowed positional args,
#793 empty ops=0 publishes, next-channel stranding (above).

**SHIP WITH KNOWN-LIMITATIONS NOTE:** #672 SIGKILL orphan locks, #669
git-behind echo, #702 >512-key config wedge, #516 root-relative restore
paths, #518 rbox ignore glob drop, #659 oracle wedge.

- [ ] #535 echo-clobber fix (.claude/worktrees carve-out is the standing
      workaround until then).
- [x] #515 swallowed-args fix — MERGED (PR #799, 2026-08-21): central
      surplus-positional gate, 62 registry arity declarations.
- [ ] #793 empty-publish fix.
- [ ] Known-limitations section in the changelog covers the note list.
- [ ] #775 unreadable-journal repos (pegasus, savvy-core) re-defer forever
      — remedy copy owed (280's ledger); stuck-repo escalation itself
      SHIPPED (#795/#796).

## Perf targets (founder yardstick: ≤10s e2e propagation)

- [x] Sender push — MET (#661 closed at target: desktop 1.5s median, Mac
      2.7s, was 7.4/6.6). 277 soaked numbers in STATUS.
- [x] desktop→FM pull lane — MET (design 279 / #786, 2026-08-19): steady
      pull 21-22s → **8.2s field-measured**, locks 1,882 → 0. Desktop/Mac
      lanes verified unregressed same day.
- [ ] Re-run the 30-attempt propagation bench on the tag candidate and
      record the numbers in STATUS.

## Release mechanics (existing, don't skip)

- [ ] Version bump both sites (package.json + CHECKED_IN_RBOX_VERSION).
- [ ] Tag only the exact SHA with green main CI; stable tag needs the
      changelog heading first.
- [ ] Fleet upgrade + `rbox stop && rbox start` per host after publish.
- [ ] Fresh founder yes for: the tag, and separately any production
      promotion.
