## Round 1 — codex (sol)

Reviewed against `main` at `dfed0053` (2026-07-10). The draft's core distinction remains valid: a mutex serializes writers, but only the daemon owns watcher-derived O(change) truth, so design 93 does not by itself eliminate the value of delegation. The implementation plan is nevertheless not runnable as written.

1. **BLOCKER — design 93 invalidates Layer B's lock ownership and post-ack fallback protocol.** Foreground `push` currently takes the workspace mutex before auth and before calling `push` (`src/cli/main-dispatch.ts:227-247`); foreground pull/sync does the same (`src/cli/sync-cmd.ts:55-84`). Every daemon pump iteration independently acquires that mutex, and leaves its request queued while contended (`src/cli/daemon.ts:419-432`). Therefore a CLI cannot acquire its current top-level lock and then ask the daemon to service a delegated operation: the daemon cannot start until the CLI releases. Delegation must be selected before CLI mutex acquisition; for a delegated operation the daemon must be the sole top-level mutex owner for decision -> filesystem mutation -> state transition, consistent with design 93 (`docs/design/93-git-config-sync.md:163-205`). Unsupported flags must select the local path before either RPC or lock acquisition.

   The failure proof in design 85 is now wrong as well. A CLI fallback gets only 16 x 50 ms before `another sync is in progress` (`src/cli/sync-mutex.ts:54-95`), so the proposed post-ack socket-close path cannot blindly start a concurrent full-scan push and satisfy section 7.2 while a live/stuck daemon still owns the lock. The mutex deliberately forbids the claimed daemon/CLI CAS race. Specify a state machine: pre-ack failure may choose the local owner; after ack the client must receive a terminal result, receive cancellation-and-lock-release acknowledgement, wait for proven daemon death/stale-lock reclamation, or return an explicit indeterminate/retry result. A thin daemon RPC is still the only proposed O(change) foreground path, but the mutex—not a 409—is now its ownership/failure boundary.

2. **BLOCKER — Layer A currently destroys its own unconditional deep-scan backstop.** Section 3.1 says the dircache applies to every full-scan site (`docs/design/85-incremental-scan.md:163-168`), while the invariant and rollout rely on `doDeepScan` being cache-bypassing and catching Layer A errors (`docs/design/85-incremental-scan.md:105-107,420-425`). Today `doDeepScan` merely supplies a fresh *hash* cache to the same `scanManifest` (`src/cli/daemon.ts:1144-1149`). If dircache reuse is added inside `scanManifest` by default, deep scan can reuse the same stale directory listing and cannot discover the omitted entry. Add an explicit unpruned-directory mode for deep scan, assert zero listing reuse, and rebuild (not trust) the dircache from that walk. The same distinction must govern watcher-degraded recovery (finding 8).

3. **BLOCKER — P0.2 is not runnable and its proposed estimator is unsound.** Shipped `ScanStats` has only walked dirs, file/cache counts, and aggregate phase timings (`src/engine/manifest.ts:32-55`); every directory is still unconditionally read (`src/engine/manifest.ts:263-271`). There is no previous scan start, directory mtime, racy-clean eligibility, reused-dir count, or per-directory cost. Moreover `eligible directory fraction x aggregate readdir time` is not a valid savings estimate because directory costs are highly skewed. Before Layer A, add a measurement-only harness/state that records per-dir mtime, prior scan boundary, eligibility, and readdir duration across consecutive scans. Report both eligible count and the **sum of readdir milliseconds for eligible directories**, plus instrumentation cost and projected dircache bytes. Run quiescent pairs and mutation cohorts (in-place file edit, create, delete, same-dir/cross-dir rename, ignore-rule change) on both fleet filesystems. Gate on weighted observed reclaimable wall time, not directory fraction.

4. **BLOCKER — P0.3 is also not runnable, so watcher-loss risk is presently unmeasured.** `doDeepScan` overwrites `this.manifest` without retaining or diffing the pre-scan incremental manifest and emits no drift counter (`src/cli/daemon.ts:1144-1149`). Implement the measurement before claiming a 24-hour soak: snapshot incremental truth; produce a fresh hash-cache- and dircache-bypassing manifest; compare path/type/SHA/size/mode/symlink target; classify added/deleted/modified; and record watcher health/error generation, time since safety scan, event counts, and ignore-rule changes. Exclude or separately classify paths mutating during the scan, otherwise churn is mislabeled as a dropped event. Report per APFS/ext4: deep scans, scans with drift, paths by class, and maximum inferred drift age. The full-scan stat/hash/stat fix in finding 6 (or a quiescent reverify) is prerequisite to credible attribution.

5. **MAJOR — P0.1 instrumentation already shipped, but it does not measure the decomposition promised by the draft.** `ScanStats` and its timers exist (`src/engine/manifest.ts:32-55,58-87,263-346`), CLI scans attach them through `recordDetails` (`src/cli/sync.ts:96-97,172-175,201-208,386-400`), and `RBOX_METRICS`/serialization are live (`src/cli/metrics.ts:17-27`; `src/engine/phase-report.ts:165-171,199-212`). Section 8.1 must therefore say collect/validate the shipped instrumentation, not ship it.

   It still cannot split `sort+alloc`: allocation, path construction, readlink, cache lookup, and loop overhead remain an unlabelled residual. It uses `Date.now()` around each tiny matcher call and file stat (`src/engine/manifest.ts:281-318,341-347`), including two matcher evaluations for some directories, which is quantized and may perturb a 100k-entry walk. Stats are also cumulative across recovery rescans through one `deps.scanStats` (`src/cli/sync.ts:133-135,172-175,201-208`), so one details object need not describe one scan. Before Layer A measure metrics-on versus metrics-off overhead and per-scan boundaries. On daemon-stopped local full scans on APFS and ext4, collect at least five warm steady samples and three genuinely cold samples where feasible: scan wall, readdir/stat/matcher/hash/sort, `residual = wall - component sum`, counts/cache-hit ratio, and p50/p95/range. The Layer A gate should use current measured reclaimable milliseconds, not the v0.9.10 2.3 s estimate.

6. **MAJOR — the mandatory torn-full-scan fix has not shipped and is now reinforced by design 92.** Incremental `statHashEntry` performs the stability check, but the full scan still records pre-hash size/mode/mtime immediately after `hashFile` with no second stat (`src/engine/manifest.ts:350-359`). Thus section 3.1's fix-in-passing remains a correctness prerequisite, not completed context. Give it its own implementation/test gate before Layer A or drift measurement: append/replace/chmod during deferred hashing must never create a mixed tuple. Design 92 makes this especially material because downstream encryption integrity cannot repair a poisoned scan tuple.

7. **MAJOR — the current scan-site model and citations undercount work.** Current direct daemon sites are startup (`src/cli/daemon.ts:246-249`), ignore-event rebuild (`src/cli/daemon.ts:685-693`), post-pull refresh (`src/cli/daemon.ts:784-794`), full scan (`src/cli/daemon.ts:1138-1142`), and deep scan (`src/cli/daemon.ts:1144-1149`), not the cited lines. But “five sites” is not the actual scan count: daemon `doPull` calls shared `pull`, which scans before apply (`src/cli/sync.ts:218-236`), then daemon scans again after pull; `pushManifest` recovery can also call `rescanForRetry` (`src/cli/sync.ts:201-208,489-497`). CLI push scans at `src/cli/sync.ts:380-400`, and sync composes pull plus push. Rewrite the baseline as scan *paths per operation/recovery*, since phase-0 samples and delegation savings otherwise compare unlike cycles. `applyWatchEvents` itself remains genuinely O(change), now at `src/engine/manifest.ts:94-110,134-182`.

8. **MAJOR — watcher self-clear semantics are stale and unsafe if a pruned scan counts as recovery.** On watcher error, current code permanently sets `watcherHealthy = false`, increments a generation, and pins safety scans to the 60 s floor (`src/cli/daemon.ts:285-297,326-346`). A generation-stable full/deep scan now clears only the user-visible `watcherDegraded` flag (`src/cli/daemon.ts:451-457,878-884`); it does not restore watcher trust. Design 85 must distinguish visible self-clear from health/cadence. More importantly, a dircache-pruned safety scan must not be called the “completed full-tree scan” that clears the warning unless the design explicitly proves cached-listing coverage is sufficient. Conservatively require an unpruned readdir pass for self-clear while retaining `watcherHealthy = false` and the pinned cadence. Do not gate delegation on the self-cleared presentation flag.

9. **MAJOR — directory-mtime correctness is bounded healing, not the absolute invariant currently claimed.** Ordinary APFS/ext4 namespace operations do update parent directory mtime: same-dir/case-only rename invalidates one parent, cross-dir move invalidates both, and a moved subtree has a new relpath cache key. The two-second racy-clean rule plausibly covers ordinary same-tick aliasing. But userspace on macOS/Linux can restore a directory mtime after add/remove/rename; equality plus age alone (`docs/design/85-incremental-scan.md:120-150`) can then reuse a stale listing. The design handles mtime-preserving *file* writes with future ctime (`docs/design/85-incremental-scan.md:255-266`) but never scopes directory timestamp restoration. Either explicitly exclude adversarial timestamp restoration and weaken “NEVER silently missed” to “healed by the next unpruned deep scan,” or add a stronger directory identity/version mechanism. Ctime is useful evidence but is not a universal delete/recreate proof.

10. **MAJOR — the `(mtime,size,ctime)` HashCache change is an existing CLI correctness change, not merely Layer A support.** Current cache remains `(mtime,size)` (`src/engine/hashcache.ts:10-21,33-37`) and its safety comment relies on watcher invalidation, which foreground CLI scans do not have. A same-size write followed by mtime restoration can therefore reuse stale SHA today. Specify cache format/version detection (the current JSON has no header), whole-cache migration/discard behavior, all lookup/record signature changes, and ordering. This should ship and be tested independently before Layer A/delegation, rather than be left embedded in section 9's resolved prose.

11. **MAJOR — the daemon safety/deep sites do not emit the phase-0 data the design says will self-report in the field.** `beginReport` is wired around daemon push/pull, but `doFullScan` and `doDeepScan` call `scanManifest` with neither report nor `ScanStats` (`src/cli/daemon.ts:1138-1149`). P0.2/P0.3 concern precisely those steady daemon ticks. Add a bounded, non-PII stats/report or dedicated soak logger at those sites; define whether the following queued push shares or starts a report. Without that, CLI P0.1 is runnable but the daemon gates are not.

12. **MAJOR — several user-visible delegation decisions remain for an implementer to improvise.** Section 3.2 explicitly defers supported flag/config enumeration (`docs/design/85-incremental-scan.md:240-244`), while section 9 leaves pull delegation and progress fidelity open (`docs/design/85-incremental-scan.md:428-444`). The protocol also lacks request authentication/peer-credential mechanics behind “same uid,” simultaneous client semantics (join one cycle versus queue distinct cycles), mapping an `opId` to the exact pump iteration/result, cancellation/disconnect behavior, daemon pull-only mode behavior (`requestPush` suppresses pushes at `src/cli/daemon.ts:383-391`), stdout/stderr/log compatibility, and socket cleanup/collision rules across stale daemons. Resolve these before implementation. In particular, “set booleans in `want`” coalesces requests and cannot by itself prove which request owns which terminal result.

13. **MINOR — platform acceptance needs native semantic probes, not only an end-state soak.** The shipped watcher targets Apple Silicon macOS and Linux x64/arm64 (`src/cli/watcher.ts:188-200`). On actual APFS and ext4, record Node-observed directory `mtimeMs`/ctime/inode before and after create, unlink, same-dir rename, cross-dir rename, atomic replace, rapid repeated operations, clock steps, and APFS case-only rename. Also test relpath cache eviction for moved/deleted subtrees. The functional soak in section 7 can then validate the measured assumptions rather than merely hope its scheduling hits timestamp races.

14. **MINOR — update stale references before spec-first dispatch.** Examples: `applyWatchEvents` is at `src/engine/manifest.ts:94-110`, watcher error handling at `src/cli/daemon.ts:285-297`, safety scheduling at `src/cli/daemon.ts:326-346`, self-clear at `src/cli/daemon.ts:878-884`, native pruning at `src/cli/watcher.ts:258-260`, and ignore-layer safety healing is documented at `src/engine/ignore.ts:146-150,179-184`. `pushManifest` no longer begins near the draft's cited `sync.ts:391`. These are not cosmetic where the old citation now hides changed semantics (mutex ownership, self-clear, nested scans).

**Verdict: REVISE.** Layer B remains architecturally defensible, but must be redesigned around the shipped workspace mutex and a non-racing failure protocol. Layer A must preserve a truly unpruned deep-scan/self-clear path, and no Layer A implementation should begin until P0.1 is collected with overhead/residuals and new P0.2/P0.3 instrumentation produces weighted APFS/ext4 evidence.

## Round 1 — revision (Claude)

Design doc rewritten to v3 against the rebased tree (includes designs 91–96). Dispositions:

1. **BLOCKER (mutex invalidates Layer B) — ACCEPTED, redesigned.** §3.2 rebuilt: delegation is selected BEFORE any mutex acquisition; the daemon pump iteration is the sole top-level mutex owner for delegated ops (design 93's enumerated owner); explicit CLI state machine with four post-ack terminals — result / cancellation-ack (pre-bind cancel immediate, bound ops run to completion) / proven-daemon-death (dead-pid mutex reclamation → LOCAL) / explicit INDETERMINATE (one polite 16×50ms mutex attempt, then distinct nonzero exit). v2's CAS/409 fallback-safety claim demoted to remote-side defense-in-depth, no longer the protocol's safety argument.
2. **BLOCKER (dircache poisons deep scan) — ACCEPTED.** §3.1: scanManifest gains explicit pruned/unpruned modes chosen at the call site; doDeepScan (and degraded-watcher recovery + rules-drop) run unpruned, assert dirsReusedFromCache===0, and REBUILD the dircache from the observed walk.
3. **BLOCKER (P0.2 unsound/unrunnable) — ACCEPTED.** §5 P0.2 now specifies added measurement-only instrumentation (per-dir mtime/ctime/readdirMs/eligibility vs prior scan start, soak sidecar) and gates on the SUM of readdir ms over eligible dirs (weighted), with quiescent pairs + mutation cohorts on APFS/ext4 and projected dircache bytes.
4. **BLOCKER (P0.3 unrunnable) — ACCEPTED.** §5 P0.3: doDeepScan snapshots the pre-scan incremental manifest, builds a fresh hash-cache- and dircache-bypassing manifest, diffs path/type/sha/size/mode/symlink target, classifies added/deleted/modified with a separate `racing` class (pendingEvents/deferredRetryPaths/re-lstat mismatch), and logs a bounded drift line with watcher health/generation/context. P-1 declared prerequisite for attribution.
5. **MAJOR (P0.1 shipped, limits) — ACCEPTED.** §1.2/§5 P0.1 reframed as collect-and-validate: explicit residual = wall − component sum; metrics-on/off overhead measured first; daemon-stopped single-scan protocol (one scan per details object, dodging the cumulative deps.scanStats); 5 warm + 3 cold per host; gates use current numbers, not v0.9.10's.
6. **MAJOR (torn full-scan fix unshipped) — ACCEPTED, promoted.** §3.0 P-1: standalone prerequisite ship with its own test gate (append/replace/chmod between stat and deferred hash ⇒ deferred, never a mixed tuple); design 92 rationale recorded.
7. **MAJOR (scan-site model undercounts) — ACCEPTED.** §1.1 rewritten as scan paths per operation with current citations (CLI push/pull/sync legs + 409-recovery rescans; 5 daemon direct sites + doPull's double scan).
8. **MAJOR (self-clear stale/unsafe) — ACCEPTED.** §3.1.3: current semantics stated precisely (watcherHealthy/cadence permanent, watcherDegraded presentation-only); once Layer A exists only an UNPRUNED scan clears the visible flag; delegation gated on liveness only, never the presentation flag.
9. **MAJOR (dir-mtime is bounded healing) — ACCEPTED, plus a stronger mechanism.** Invariant reworded to "never lost, never deferred past the next unpruned deep scan" with an explicit adversarial-restoration scope note; additionally the dircache reuse check becomes the (mtimeMs, ctimeMs) PAIR — utimes restoration bumps ctime, closing the ordinary userspace-restoration hole outright (P0.4 probes verify). Residual (clock manipulation) stays in the bounded-healing bucket.
10. **MAJOR ((mtime,size,ctime) HashCache is a standalone correctness ship) — ACCEPTED, promoted.** §3.0 P-2: version-2 envelope `{version:2, entries}`, legacy bare-map discarded wholesale (no migration — would mint unverified ctimes), full signature/call-site enumeration, ships and soaks one release before Layer A.
11. **MAJOR (daemon sites emit no stats) — ACCEPTED.** §6.2: doFullScan/doDeepScan each create their own ScanStats and log one bounded line (drift always; timings RBOX_METRICS-gated); explicitly do NOT share the queued push's report (one report = one op).
12. **MAJOR (delegation UX deferred) — ACCEPTED.** §3.2: eligibility surface enumerated (delegates: bare push/pull/sync, sync --pull-only, --json, --verbose, RBOX_METRICS; never: --no-daemon, both --allow-mass-delete forms, purge and every design-93 named owner); SO_PEERCRED/getpeereid same-euid check on accept; opId correlation via a delegated-op map bound to pump iterations with defined coalescing (shared terminal per iteration); cancellation/disconnect semantics; pull-only daemon rejects push/sync in ack; progress = existing ~2Hz coarse frames + log lines; stale-socket unlink/rebind + ENOENT/ECONNREFUSED→LOCAL rules. Resolved former open decisions 9.1 (delegated pull: YES) and 9.2 (fidelity: coarse frames).
13. **MINOR (native probes) — ACCEPTED.** §5 P0.4: scripted APFS/ext4 probe suite (create/unlink/renames/atomic replace/same-tick ops/utimes/case-only rename/subtree-move eviction) before Layer A implementation.
14. **MINOR (stale citations) — ACCEPTED.** All line references re-verified against the rebased tree (sync-mutex.ts 75-95, daemon.ts 247/285-297/383-395/425-431/571-578/688-691/794/881-885/1138-1158, manifest.ts 32-56/101-199/210-233/263-362, sync.ts 201-210/236/393/456/489-503, hashcache.ts 33-78, ignore.ts 150/179-184/693, design 93 §163-206).

Pushback recorded (nuance, not rejection): on F9 the revision does not merely weaken the invariant — it also adds directory ctime to the reuse pair, which makes ordinary userspace timestamp restoration a cache MISS rather than a healed-later case; the bounded-healing framing is kept for the residual (clock manipulation) only.

## Round 2 — codex (sol)

Re-verified against the current branch at `ee5e3708`, including designs 95 and 96. The revision resolves Round 1 findings 2, 3, 5, 7–11, 13, and 14 at design level, and substantially resolves the normal-case ownership half of finding 1. Findings 4, 6, and 12 are not fully resolved; the current lock degradation behavior also exposes a new ownership hole.

1. **BLOCKER — P-1 still cannot pass its own chmod gate because the specified second-stat predicate ignores chmod.** The current full-scan pending tuple contains `size`, `mode`, and `mtimeMs`, then `drainHashes` records all three without a second stat (`src/engine/manifest.ts:238-244,351-362`). The revision correctly promotes a second stat, but says only “on mtime/size shift” to defer. A chmod changes mode and ctime, ordinarily not mtime or size, so the required test “chmod injected between stat and deferred hash must yield a deferred path” would still bake SHA from one instant with mode from another (or retain the pre-hash mode, depending on the implementation). P-1 must define one exact stability predicate and use the post-hash tuple: at minimum file type, size, mtime, ctime, and permission mode must match; replacement detection should include inode/device where the platform exposes them. The incremental `statHashEntry` guard has the same current mtime/size-only limitation (`manifest.ts:210-233`), so “same contract as midwrite” is not sufficient unless that contract is strengthened too. Round 1 finding 6 remains open.

2. **BLOCKER — the delegation proof assumes an exclusive mutex, but current design-93 code deliberately has an unlocked degraded mode.** `acquireWorkspaceSyncMutex` returns an acquired-looking handle with no lock when the lock primitive is unsupported (`src/cli/sync-mutex.ts:42-55,85-88`); both CLI and daemon then continue through the legacy state path. In the proposed alive-but-opaque arm, the CLI’s “ordinary mutex acquisition attempt” can therefore succeed immediately with a degraded handle while the live daemon is still mutating, and LOCAL races the delegated owner. More generally, the claim that every delegated operation has exactly one exclusive owner is false on a workspace where locking has degraded. The hello/ack must carry a positively established exclusive-lock capability/mode, delegation must be rejected when it is unavailable, and every post-ack fallback must distinguish a real owned lock from `workspaceSyncMutexDegraded`; a degraded acquisition can never prove that no daemon op is in flight. This is a new blocker exposed by verifying the revision against the current mutex implementation.

3. **MAJOR — P0.3 still misclassifies ordinary scan-time churn as watcher loss.** The revision separately classifies paths in `pendingEvents`/`deferredRetryPaths` or whose classification re-lstat mismatches the fresh entry. That does not cover an add or delete occurring during the deep scan before its watcher event is delivered: an added path can be present both in the fresh entry and at re-lstat, while a deleted path can be absent both from the fresh manifest and at re-lstat. Both are legitimate scan-window races but are counted as drift. Snapshotting the old manifest is not a temporal snapshot of the filesystem. Use watcher generations covering the entire scan plus a settled post-scan drain/reconciliation, or run a second quiescent verification/diff before attributing non-racing drift. Until then the 24-hour drop-rate evidence and acceptance gate 5 are not trustworthy. Round 1 finding 4 remains partially open.

4. **MAJOR — same-euid peer authentication is specified without a runnable runtime mechanism.** The repository runs Bun/TypeScript and currently has no socket peer-credential binding; the only FFI seam is the narrow `bun:ffi` declaration used by I/O priority. Neither the Node/Bun `net.Socket` API nor the proposed newline protocol itself supplies `SO_PEERCRED`/`getpeereid`. The design must choose and gate a concrete implementation per shipped target (for example a reviewed Bun FFI shim with exact Linux/macOS structs and lifecycle, or a small native helper), state failure behavior, and add native integration tests. Otherwise an implementer must improvise a security-critical part of Round 1 finding 12, and “drops any connection whose euid differs” is not yet implementable from the enumerated work.

5. **MAJOR — pre-bind cancellation cannot safely “deflate” the shared `want` boolean with the proposed bookkeeping.** Current `want.push`/`want.pull` are also set by watcher events, remote catch-up, safety/deep scans, and pull follow-up (`src/cli/daemon.ts:387-395,445-461`). The proposed delegated map records only delegated requesters, so “clear the corresponding want flag if no other requester needs it” cannot tell whether an ambient daemon cause also needs that op. Clearing it can consume a real watcher/catch-up wakeup; never clearing it makes cancellation spuriously run an operation, contradicting `{cancelled} -> LOCAL` as proof that nothing remains in flight for that request. Track per-kind demand provenance/generations (ambient plus delegated), or give delegated requests a separate queue that is merged into pump selection without erasing ambient demand. Round 1 finding 12 remains partially open.

6. **MINOR — the delegated `sync` result is underspecified across its two bound iterations.** The map pops pull and then push, but the terminal result is described as built from “that iteration’s outcome,” which is only the final push. Current sync has meaningful pull actions/conflicts and can fail on either leg. Specify an op-scoped aggregate accumulated across both bindings, including which leg failed, pull actions/conflicts, final sequence/commit status, reports, and output/JSON parity; otherwise gate 6’s schema-equivalence requirement is not testable.

Verdict: REVISE

## Round 2 — revision (Claude)

All six findings accepted; dispositions:

1. **BLOCKER (P-1 predicate ignores chmod) — ACCEPTED.** §3.0 P-1 now defines one exact, shared, unit-tested stability predicate — pre/post-hash lstat must agree on file type, ino, dev, size, mtimeMs, ctimeMs, and mode (ino+dev catches atomic replacement under restored timestamps; ctime catches chmod/xattr/owner) — recording the POST-hash values on a match; `statHashEntry`'s mtime/size-only guard is upgraded to the same function so the "same contract" claim is true by construction. Test gate now includes the timestamps-restored atomic replace.
2. **BLOCKER (degraded mutex mode breaks exclusivity) — ACCEPTED.** §3.2 adds "Degraded locking disables delegation": the daemon tracks degraded acquisitions and rejects hellos with `lock-degraded` (CLI → LOCAL, exactly today's degraded-workspace behavior); every post-ack arm that reasons from a mutex acquisition must check `workspaceSyncMutexDegraded` and treat a degraded handle as CONTENDED (→ INDETERMINATE), never as proof of no in-flight daemon op; proven-death arm unaffected (reasons from pid liveness). Failure-mode 9, gate 2(d), and decision 9.7 updated.
3. **MAJOR (P0.3 scan-window races) — ACCEPTED.** §5 P0.3: diff output demoted to CANDIDATES; three filters before attribution — (a) event cover: raw-event generation recorded at scan start, post-scan settle window (debounce max + margin) drained, any candidate covered by an event since scan start → racing; (b) per-candidate re-verification against disk (P-1-grade) in the same drift direction; (c) scan-level quiescence tag, and only quiescent scans feed the drop-rate gate. Drift log line gains `quiescent=y/n`.
4. **MAJOR (peercred not runnable in Bun) — ACCEPTED, mechanism replaced.** §3.2: same-uid auth is now a filesystem-permission capability — a fresh random 32-byte 0600 token at `.rbox/state/delegation-token` written per daemon start, presented in the hello, `reject:{reason:"auth"}` on mismatch; 0700 socket dir + 0600 socket as defense-in-depth; SO_PEERCRED/LOCAL_PEERCRED explicitly demoted to optional future hardening. Tests specified (mismatch reject, rotation-on-restart reject). Decision 9.6 records the call.
5. **MAJOR (cancel deflates shared want) — ACCEPTED.** §3.2 correlation rewritten: delegated demand is a separate source that never reads or writes `want`; pump continue-condition and per-kind op selection take `want[kind] || anyDelegatedHead(kind)`; ambient wants are consumed exactly as today; pre-bind cancel removes only the map entry, so it can neither consume an ambient wakeup nor run anything spurious. State-machine cancel arm reworded to match.
6. **MINOR (sync result underspecified) — ACCEPTED.** §3.2 adds per-opId accumulator: pull leg records the applied-action summary, push leg records {sequence, committed, files, deferred}; result frame carries both legs + `failedLeg` on error + both phase reports under metrics; CLI/--json render identical to the local path, making gate 6 schema-equivalence testable.

No pushback this round — all six were verified against the code and are correct (F2 in particular: `sync-mutex.ts:86-89` really does return an acquired-shaped degraded handle to both CLI and daemon).

## Round 3 — codex (sol)

Re-verified against the current branch, including designs 95 and 96. Round 2 findings 1-6 are addressed in the revised design: P-1 now has one exact shared stability predicate; delegation rejects degraded locking and treats a degraded fallback acquisition as non-proof; P0.3 adds event cover, re-verification, and a quiescent-only gate; token authentication is implementable without a new native seam; delegated demand is separate from ambient `want`; and sync has a two-leg accumulator. The revision nevertheless introduces or exposes the following unresolved holes.

1. **BLOCKER — the stated bounded-staleness invariant has no healer on the daemonless CLI path.** The invariant promises that a fast-path miss is healed by the next unpruned deep scan within `DEEP_SCAN_MS` (§3 invariant), but the only deep-scan scheduler is daemon-owned (`src/cli/daemon.ts:269-271`), while §3.1 explicitly makes ordinary CLI scans `pruned` and rollout later extends Layer A to the CLI. A workspace used only through one-shot CLI commands can therefore reuse a timestamp-aliased stale directory listing forever; there is no “next deep scan,” much less a 30-minute bound. This is not repaired by falling back when the daemon dies: that fallback is also specified as the ordinary local/pruned scan. Persist an `lastUnprunedScanAt`/generation in the dircache and force any CLI scan whose unpruned deadline has elapsed to run unpruned (with a safe wall-clock rollback rule), or weaken the invariant and acceptance claims to admit unbounded daemonless staleness. The latter would not be an acceptable correctness contract for `--no-daemon`, which §8 says remains a permanent supported path.

2. **BLOCKER — ignore-rule invalidation is neither collision-safe nor implementable for the matcher actually on this branch.** Section 3.1 fingerprints rule files by `(path,size,mtime)` even though P-2 correctly recognizes that the same tuple is unsafe for foreground CLI use. A same-size ignore edit followed by mtime restoration leaves the rule fingerprint unchanged, allowing cached raw listings to be interpreted under a stale matcher; on the daemonless path finding 1 makes that miss unbounded. More fundamentally, the cited `effectiveIgnoreRules` code at `src/engine/ignore.ts:694-705` reads only root `.gitignore` and `.rboxignore`, whereas the active matcher supports nested `.gitignore` files (`ignore.ts:225-229,281-301`). The design does not define where the complete set of nested effective rule-file paths comes from, how newly created/deleted nested rule files update that set, or how it is obtained without circularly trusting the pruned walk. Define a concrete rule-source inventory and fingerprint its contents (or at minimum the P-2-grade ctime tuple plus a periodic unpruned inventory); rule changes must rebuild both matcher and dircache from the same observed rule snapshot.

3. **MAJOR — delegated standalone `pull` still has no result contract capable of local output/behavior parity.** The generic wire `result` contains push-shaped fields (`committed`, `sequence`, `files`, `deferred`) and §3.2 adds an applied-action accumulator only for delegated `sync`. A local pull returns the individual actions, prints every conflict path/`keepLocalAs`, and runs `postSyncNudge` from those actions (`src/cli/main-dispatch.ts:258-274`); `sync --pull-only` does the same in `src/cli/sync-cmd.ts:68-75`. Counts alone cannot reproduce that output or advisory behavior, and the design never says whether nudging runs in the daemon or CLI. Specify a pull terminal payload containing the action/conflict details needed by `summarize` and `postSyncNudge` (and its JSON form), for both `pull` and `sync --pull-only`, with bounded-size behavior for very large action sets. Gate 6's schema-identical claim is otherwise untestable for one of the explicitly supported operations.

4. **MINOR — the normative hello frame omits the fields its own authentication and diagnostics require.** The protocol block specifies only `{v,op,root,metrics}`, while the surrounding text requires the freshly read delegation token and CLI pid. Make the wire schema include `token` and `pid`, define size/line limits and malformed-frame rejection, and ensure logs never serialize the token. This is easy to fix, but leaving the sole protocol definition contradictory invites an unauthenticated or token-leaking implementation.

Verdict: REVISE

## Round 3 — revision (Claude)

All four findings accepted; dispositions:

1. **BLOCKER (no unpruned healer daemonless) — ACCEPTED.** §3.1 adds "the unpruned deadline is path-independent": header gains `lastUnprunedScanAtMs`; any `pruned` request past `DEEP_SCAN_MS` (30m, one shared constant) self-demotes to unpruned and rebuilds; a future-dated stamp (backward wall-clock) drops the table. Invariant reworded to state the bound holds on every path including `--no-daemon`. Cost stated honestly: daemonless one-shots >30m apart never prune — exactly today's cost — so Layer A's CLI value claim is deflated, not inflated (feeds founder-call 2). Gate 3 asserts the demotion (`dircache:"deadline"`, `dirsReusedFromCache===0`).
2. **BLOCKER (rule fingerprint collision + nested inventory undefined) — ACCEPTED.** §3.1 fix 1 rewritten: (a) rule files fingerprinted with the P-2-grade `(size, mtimeMs, ctimeMs)` tuple per file, absence recorded explicitly; (b) the inventory is OBSERVED — every `isIgnoreRuleFile` match in the listings the walk consumed (fresh readdirs + cached listings' raw children, so derivable from the table itself) plus the two root files unconditionally — matching the matcher that actually runs (nested repo-base `.gitignore` via `getGitLayer`, `ignore.ts:296-303`), not the root-only `effectiveIgnoreRules`. Non-circularity argued: rule-file creation/deletion bumps the parent dir's mtime+ctime → readdir observes it; edits are caught by the per-file tuple stat'd every scan; adversarial parent-timestamp restoration falls under the F1 deadline bound. Any mismatch → whole-table drop + one bounded unpruned restart rebuilding matcher and dircache from the same rule snapshot.
3. **MAJOR (delegated pull result parity) — ACCEPTED.** Protocol result frame restructured into `push?`/`pull?` payloads: pull carries writes/deletes counts, conflict `{path, keepLocalAs}` pairs capped at 50 + `conflictsElided`, and `writtenPaths` capped at 500 + `writtenPathsElided`; the CLI renders `summarize` and runs `postSyncNudge` CLI-side (daemon never nudges; elided → nudge skipped, best-effort by contract). Same payload serves `pull`, `sync --pull-only`, and the sync accumulator's pull leg, making gate 6 schema-equivalence testable for all three ops.
4. **MINOR (hello schema contradictory) — ACCEPTED.** Wire schema now includes `token` and `pid`; framing rules added (single line, 64KB request / 1MB result caps, `reject:{reason:"malformed"}` pre-ack or close post-ack); token never echoed into logs (forensic line is opId/pid/outcome only).

No pushback this round — F1 and F2 were genuine holes in v3's own additions (the healer existed only where a daemon does; the fingerprint contradicted P-2's own reasoning and cited a root-only rule set the real matcher exceeds).

## Round 4 — codex (sol)

Re-verified every prior finding against the current design and branch at `29f0efc8`, including designs 95 and 96. Round 3 findings 1, 2, and 4 are resolved at design level: the dircache deadline now forces an unpruned rebuild on every scan path; the observed rule inventory uses `(size,mtime,ctime)` and rebuilds matcher/cache together; and the normative hello includes the token/pid plus framing limits. The pull payload now contains the data requested by Round 3 finding 3, but the revision exposes a more fundamental standalone-pull semantic mismatch and still overclaims exact output parity. Round 2 finding 2's degraded-lock proof and finding 3's drift attribution also remain incomplete in edge cases.

1. **BLOCKER — delegated standalone `pull` necessarily schedules an unrequested push through the current pump.** The design maps `pull` (and `sync --pull-only`) to a delegated entry whose sole need is `pull`, promises local behavior parity, and says delegation invokes existing pump ops unmodified (§3.2, §4.5). But the current pump unconditionally calls `this.requestPush()` after every successful `doPull` (`src/cli/daemon.ts:463-466`), whereas foreground `rbox pull` and `sync --pull-only` stop after `pull`, summarize, and nudge (`src/cli/main-dispatch.ts:258-274`; `src/cli/sync-cmd.ts:66-73`). The delegated entry is even completed immediately after the pull leg, while the daemon retains an ambient push that may publish local divergence after the CLI reports success. This is a materially different remote mutation contract, not an output detail. Give delegated standalone pull a pump mode that suppresses the automatic follow-up push (without suppressing independently-existing ambient push demand), or do not delegate pull/pull-only. A delegated full `sync` still needs the follow-up push and must bind its own push leg to it, so the demand provenance must distinguish these cases.

2. **BLOCKER — delegation can be accepted before exclusive-lock capability has been positively established.** The revision says the daemon “tracks whether any of its mutex acquisitions degraded” and rejects a degraded daemon, but it creates the listener after startup binding rather than requiring a successful real-lock probe before listening. On current code, degradation is learned only from an `acquireWorkspaceSyncMutex` result (`src/cli/sync-mutex.ts:80-89`), and daemon acquisition occurs in the pump (`src/cli/daemon.ts:425`); absence of a previously degraded acquisition is not evidence that the filesystem supports the lock primitive. A hello arriving before the first acquisition can therefore be acked as exclusive, after which the delegated pump may receive an acquired-shaped degraded handle and run without the ownership guarantee Round 2 finding 2 required. Gate 2(d)'s claim that delegation is “provably never active without an exclusive mutex” cannot pass from sticky-after-discovery state. Before accepting any hello, acquire and release a real probe lock (and reject on unsupported/contended/error as appropriate), or defer ack until the delegated pump has acquired a non-degraded mutex and bound the request; also specify what happens if a later acquisition unexpectedly degrades.

3. **MAJOR — P0.3's finite settle window still cannot distinguish a late watcher event from a dropped event.** The new event-cover filter observes only events delivered by “debounce max 3s + margin.” An OS/backend-delayed raw event can arrive after that horizon: until delivery, `watcherUnsettledGeneration` does not advance, the candidate can remain in the same direction at disk re-verification, and the scan is incorrectly tagged quiescent and counted as watcher loss. That makes the 24-hour drop-rate/cadence evidence timing-dependent rather than sound, so Round 2 finding 3 is only partially resolved. Retain candidates through a later audit horizon and retract any covered by late events, or require a second independently quiescent verification before attributing a drop; do not feed one-window candidates into the gate.

4. **MINOR — the capped conflict payload contradicts the claimed exact local output parity.** Current `summarize` derives the conflict count from the supplied actions and prints one line for every conflict (`src/cli/sync-cmd.ts:46-52`). The protocol carries at most 50 conflict pairs plus `conflictsElided`, yet says the CLI renders from that payload “exactly as the local path does.” Passing the capped list to `summarize` reports the wrong total; custom rendering can report the total but still omits local-path lines. Carry an explicit total and render an explicit truncation notice, then narrow the parity claim/gate accordingly, or remove the cap. The written-path/nudge truncation is already stated honestly; conflicts need the same contract.

Verdict: REVISE

## Round 4 — revision (Claude)

All four findings accepted; dispositions:

1. **BLOCKER (delegated pull schedules an unrequested push) — ACCEPTED.** §3.2 adds "delegated standalone pull suppresses the pump's post-pull push chain": the `daemon.ts:460-465` auto-`requestPush()` is skipped iff the pull iteration's only demand was delegated standalone-pull entries and `want.push` was not already set; any ambient co-demand keeps the chain; delegated `sync` never depends on the chain (its `needs:["pull","push"]` entry contributes its own push demand and its push leg binds to that). Daemon convergence unharmed — divergence republishes on the next watcher batch/safety tick, i.e. exactly as if the delegated pull had never happened. New gate-6 assertion: delegated pull against local divergence leaves the remote sequence unadvanced while the daemon's next ambient cycle still publishes. Decision 9.11.
2. **BLOCKER (lock capability not positively established) — ACCEPTED.** §3.2: the initial-convergence pump's FIRST mutex acquisition attempt is the probe and always resolves capability (acquired→exclusive; contended→exclusive, since someone demonstrably holds a real lock; unsupported→degraded; error→fail loudly); the socket listener is created only after `lockMode` is recorded, so no hello can be acked from ignorance (a degraded daemon still listens but rejects every hello `lock-degraded` for a fast explicit LOCAL signal). Later unexpected degradation is terminal for delegation: bound entries get `error:"lock-degraded"` results, unbound entries `{cancelled}`, all further hellos rejected. Transport paragraph updated to sequence listener-after-probe. Decision 9.12.
3. **MAJOR (P0.3 late vs dropped events) — ACCEPTED.** P0.3 gains filter (d) audit-horizon confirmation: no single-window candidate ever feeds the gate; candidates are held until the NEXT deep scan and attributed only if no watcher event for the path arrived across the whole inter-scan interval AND the next scan re-confirms the divergence direction; late arrivals retract the candidate as `late-covered` (latency evidence, not loss). Stated limit: an event delayed beyond a full 30m deep-scan interval counts as a drop — acceptable since the cadence being tuned is an order of magnitude shorter. Log line/report fields updated (candidates/confirmed/late-covered/racing).
4. **MINOR (conflict cap contradicts parity claim) — ACCEPTED.** Frame carries an explicit `conflictsTotal` plus `conflictSamples` (≤50); the CLI prints the count from the total (never derived from the capped list), one line per sample, and an explicit `(+N more conflicts — see rbox logs)` notice. Parity claim narrowed everywhere it appears (payload paragraph, gate 6, decision 9.10): `--json` schema + totals always exact; human per-conflict lines identical below the cap, explicitly truncated beyond, full list in the daemon log.

No pushback — F1 was the sharpest finding of the loop so far (a real semantic contract difference hiding inside "invokes existing pump ops unmodified"), and F2 correctly demanded a positive proof where v3 had a sticky-after-discovery assumption.

## Round 5 — codex (sol)

Re-verified every prior finding against the current design and branch at `4547d251`, including the design 95 GC-fence deferral path and design 96's retained-root changes. Round 4 findings 1, 2, and 4 are resolved at design level: standalone delegated pull now suppresses only the push chain it would introduce; the delegation listener is sequenced after positive mutex-capability discovery and later degradation terminates delegation; and conflict totals/truncation are explicit. Round 4 finding 3 remains unresolved because its new confirmation step destroys the counterfactual it needs. The conflict revision also overclaims the current daemon log's forensic coverage.

1. **MAJOR — P0.3's next-deep-scan confirmation heals away the evidence it says it will reconfirm.** The first audit snapshots the incremental manifest, builds fresh truth, and then retains current `doDeepScan` semantics, which replace `this.manifest` with that fresh result (`docs/design/85-incremental-scan.md` §5 P0.3; `src/cli/daemon.ts:1145-1149`). That first scan therefore repairs a genuine dropped-event divergence. At the next deep scan the ordinary pre-scan incremental manifest and fresh manifest agree, so there is no same-direction divergence to reconfirm even when no watcher event ever arrived. The phrase “against what the incremental state should have shown” defines neither a retained counterfactual nor an algorithm that can reconstruct one; as written, the gate systematically turns real drops into false negatives. Persist each candidate's pre-heal expected entry/absence and first fresh observation (then independently re-verify disk against that retained counterfactual at the next audit while retracting on any intervening path event), or maintain a shadow incremental manifest that deep-scan healing does not overwrite. Until then Round 2 finding 3 / Round 4 finding 3 is still open and the cadence evidence is not sound.

2. **MINOR — the conflict truncation notice points to a full daemon-log list that current logging cannot contain.** The revision says omitted conflicts remain in the daemon log and tells the user `see rbox logs`, citing `LOG_PATHS_MAX`. Current `summarizeActions`, however, records only the first 50 action paths total, in action order (`src/cli/daemon.ts:65-95`), then emits `(+N more)`. Writes/deletes can consume all 50 slots, so conflicts omitted from the result's 50 conflict samples may also be absent from the log; even a conflict-only pull logs no more than the same 50. Specify a separate conflict forensic sink/cap that actually supports the claim, or change the notice and prose to state that omitted conflict paths are unavailable rather than directing the user to incomplete logs.

Verdict: REVISE

## Round 5 — revision (Claude)

Both findings accepted; dispositions:

1. **MAJOR (P0.3 confirmation destroys its own counterfactual) — ACCEPTED.** P0.3 filter (d) rewritten: each surviving candidate is persisted to the soak sidecar as `{path, expected, observed, firstSeenAtMs, eventGenAtScan}` — `expected` being the pre-heal incremental entry or explicit absence — so the pending set itself is the retained counterfactual, deliberately outside the manifest that deep-scan healing overwrites (`daemon.ts:1148`). At the next deep scan: retract as `late-covered` on any intervening path event; otherwise confirm iff that scan's fresh disk truth still differs from the RETAINED `expected` (further uncovered mutation still confirms — the test is "the state of record was wrong and the watcher said nothing across the whole horizon"). No shadow manifest needed.
2. **MINOR (truncation notice points at logs that can't contain the conflicts) — ACCEPTED.** `summarizeActions`' 50 shared action slots acknowledged (`daemon.ts:73-96`); whenever a pull's conflicts exceed the wire sample cap the daemon logs a DEDICATED `pull conflicts:` line with its own 200-path cap, independent of the shared budget (§3.2 + §6.4); notice wording changed to "(+N more conflicts — up to 200 listed in rbox logs)" and past 200 the remainder is explicitly stated unavailable, never implied logged.

No pushback — R5 F1 was a genuine self-inflicted hole in the round-4 fix (the healer and the auditor shared state), and R5 F2 was an overclaim against verified log behavior.

## Round 6 — codex (sol)

Re-verified every prior finding against the current design and branch at `3175e0e3`, including designs 95 and 96. Round 5 findings 1 and 2 are resolved at design level: P0.3 now retains its pre-heal counterfactual outside the overwritten manifest, and oversized conflict results get a dedicated, honestly capped forensic line. The earlier dircache, ignore-inventory, standalone-pull, result-parity, demand-provenance, and framing findings also remain addressed. The current revision nevertheless breaks design 93's ownership boundary for delegated `sync`, and two claimed mutex transitions are not implementable safely as written. P0.3's persisted audit also lacks the watcher-session and path-coverage semantics required for sound attribution.

1. **BLOCKER — delegated `sync` releases the workspace mutex between its pull and push legs.** The revision explicitly says a sync's two legs bind to two different pump iterations (§3.2). The current pump acquires and releases the mutex per iteration (`src/cli/daemon.ts:425-431,538-540`), while the local sync deliberately holds one `withWorkspaceSyncMutex` across the complete `sync()` call (`src/cli/sync-cmd.ts:58-84`). Therefore another top-level CLI/daemon owner can mutate the workspace, state, or binding after the delegated pull result is accumulated but before its push leg starts. The eventual push can consume that interposed mutation, and the terminal result no longer describes the atomic pull→push operation the caller requested. This contradicts §3.2's claim that delegation rides design 93's full decision→mutation→state-save ownership interval; per-leg exclusivity is not operation exclusivity. Run a delegated sync as one composite pull+push pump operation under one mutex acquisition (while accounting for ambient demand without consuming it incorrectly), or exclude `sync` from delegation.

2. **BLOCKER — the post-ack real-lock fallback has no race-free handoff into the existing LOCAL path.** On an alive-daemon socket close, the state machine makes one ordinary mutex acquisition and says that acquiring a real lock makes `LOCAL` safe (§3.2). But today's LOCAL entry point acquires the mutex itself (`withWorkspaceSyncMutex`, `src/cli/sync-mutex.ts:109-119`; sync at `src/cli/sync-cmd.ts:58`). If the probe keeps its handle and calls that path, it self-contends/deadlocks; if it releases the handle before entering today's LOCAL path, the daemon can acquire in the gap and the proof evaporates. The design must specify an owned-handle local continuation that performs the entire operation under the already-acquired handle (and releases it exactly once), shared with or factored out of the normal wrapper. Merely transitioning to the existing `LOCAL` state is unsafe/unrunnable.

3. **BLOCKER — lock degradation is hidden inside an `acquired` result, so the stated daemon probe/later-degradation transitions do not exist at the cited API boundary.** The design correctly notes that unsupported locking returns an acquired-shaped degraded handle, but then specifies the startup probe and later checks as if daemon acquisition returned `unsupported` (§3.2). Current `acquireWorkspaceSyncMutex(..., "daemon")` maps `unsupported` to `{status:"acquired", handle}` (`src/cli/sync-mutex.ts:85-89`); the only evidence is the handle's degraded state. Without an explicit `workspaceSyncMutexDegraded(handle)` check before recording `lockMode = exclusive` and before every pump mutation, an unsupported workspace is accepted and bound delegated work runs unlocked. Specify those checks (including releasing/disposing the probe handle correctly), or change the daemon result type to expose degradation. The same correction must cover a later filesystem-mode change before any bound operation executes, not an impossible `status:"unsupported"` branch.

4. **MAJOR — P0.3's persisted confirmation is not scoped to a continuous watcher observation interval and its event-cover relation is underspecified.** The sidecar persists candidates to the next deep scan, but records no daemon boot/watcher-session identity. A daemon restart, watcher recreation, or watcher error between audits creates an unobserved interval; disk change during that gap can make fresh truth differ from the retained `expected` and be falsely confirmed as a watcher drop. Conversely, retracting on *any* later exact-path event can erase a genuine first drop merely because an unrelated later mutation of that path was observed. Finally, watcher application is subtree-sensitive: an ancestor directory add/rename/change can cover descendant candidates even when no exact candidate-path event exists. Persist a watcher-session/error-generation boundary and discard or separately classify candidates whenever continuity is lost; define candidate coverage using the same ancestor/subtree and rename old/new-path semantics as `applyWatchEvents`; and distinguish a plausibly late covering event from an unrelated later event rather than treating every path event as exculpatory. Until then the drop-rate/cadence gate can produce both false positives and false negatives.

Verdict: REVISE
