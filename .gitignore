# deps
node_modules/

# build output
dist/
.cache/
src/engine/generated/

# local workspace/runtime state (per-device, never synced or committed)
.rbox/

# wrangler local state
.wrangler/

# logs / os junk
*.log
.DS_Store

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example
*.local.secret
TEST_CREDENTIALS
.agents/
.claude/skills/
bench-results/
gate-artifacts/

# rig test-bench run artifacts (design 56) — logs, reports, cached image hash
scripts/rig/runs/
# Session-local artifacts — never commit (a git add -A once swept 400k lines of
# agent worktrees + local scratch into a release commit, 2026-07-13).
.claude/worktrees/
.claude/settings.local.json
HERE.md
