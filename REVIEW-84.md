## Round 1 — codex (sol)

1. **BLOCKER — The advertised broken-chain recovery cannot pass the landed head-authority gates, so one bad delta can permanently wedge every current writer.** Design 84 says a device with an intact local folded base can react to a chain-integrity pull failure by emitting a fresh snapshot on its next push (`docs/design/84-manifest-delta-encoding.md:351-368`). Current `verifiedHead()` advances the anti-rollback pin only after verifying the signed chain and terminal head, but *before* manifest decode/apply (`src/cli/e2ee-remote.ts:227-258`; decode happens later at `src/cli/e2ee-remote.ts:178-183`). If delta folding then fails, `lastSyncedSequence` remains the older applied sequence. The next commit is explicitly rejected when pin sequence != applied `parentSequence` (`src/cli/e2ee-remote.ts:411-420`), and the normal conflict loop pulls before retry (`src/cli/sync.ts:489-493`), which fails on the same broken chain. `rbox recover` does not provide the claimed escape hatch: it clears the pin and immediately pulls the server head (`src/cli/recover-cmd.ts:53-77`), so it also must decode the broken head. Specify an explicit, authenticated repair transaction that can sign a snapshot as a child of the DO-authoritative head without first applying that head (including how it obtains/verifies the parent hash, how local applied state is selected, how destructive stale-state publication is guarded, and what `recover` does). The current “automatic next push” text is impossible post-design-91/92.

2. **BLOCKER — `manifestChain` cannot establish I3 as specified: the zero-knowledge server can validate addresses, but cannot validate that the signed list is complete or corresponds to the encrypted delta chain.** I3 promises every link is GC-rooted (`docs/design/84-manifest-delta-encoding.md:106-111`), while §3.5 only shape/presence-checks a client-supplied list (`docs/design/84-manifest-delta-encoding.md:292-330`). Because the actual `baseEncSha` links remain encrypted, a buggy or hostile writer can omit a required base from `manifestChain`; the commit is accepted, later GC sees no root, and the live head becomes unreadable. Current GC is deliberately fail-closed only over what the DO can enumerate: `roots()` parses signed bodies and emits `encManifestSha` plus data refs (`apps/api/src/workspace-sync.ts:474-514`), and Phase 1 treats precisely that union as authoritative (`apps/api/src/gc-phase1.ts:28-42`). The protocol must require readers/writers to compare the decrypted chain exactly against the signed `manifestChain` (order, terminal snapshot, every immediate `baseEncSha`, no extras), define the failure before pin advancement, and accurately downgrade server enforcement to syntactic/presence enforcement. If buggy signed writers are in scope for I3, a server-visible parent-link field (or another enforceable construction) is required; the present design cannot make its stated invariant true.

3. **BLOCKER — The envelope wire format and compatibility discriminator are not implementation-ready.** The design calls the plaintext “self-describing” but gives no envelope version/magic, no strict allowed-field schema, and no normative legacy-reader rule (`docs/design/84-manifest-delta-encoding.md:95-100,133-160`). Phase B says readers support raw legacy manifests plus envelopes (`docs/design/84-manifest-delta-encoding.md:493-505`), yet does not say whether any object with `kind` is an envelope, how a legacy manifest containing an unknown `kind` property is treated, or how unknown future envelope versions fail. The compressed snapshot example places a “zstd payload” in a JSON `manifest` field without defining its JSON representation, uncompressed-length cap, compression-bomb limit, or whether `manifestHash` covers decoded JCS or original serialized bytes (`docs/design/84-manifest-delta-encoding.md:140-160`). Current encrypted blobs are exact opaque bytes (`src/engine/e2ee/manifest-crypto.ts:32-47`), and current decode is unambiguously raw JSON -> `Manifest` (`src/cli/e2ee-remote.ts:205-215`). Choose a versioned type (for example an unambiguous `type: rbox/manifest-envelope/v1`), fully specify raw-v0 discrimination and strict parsing, and define the compressed byte encoding and bounds before dispatch.

4. **MAJOR — The re-baseline section silently weakens design-91 continuity and is not reconciled with either current code or `rbox recover`.** Design 84 proposes accepting a merely signature-valid newer head while skipping unverifiable ancestry and says this is “exactly what [re-baseline] gives up today” (`docs/design/84-manifest-delta-encoding.md:397-423`). That is not current reality: `verifiedHead()` always calls `commitsSince(pinnedSeq)` and verifies the full forward chain to the exact `/latest` terminal hash (`src/cli/e2ee-remote.ts:227-258`); a `NeedsRebaselineError` is not caught there. The only catch is the retained-history helper, which searches for a segment still anchored at the already verified head (`src/cli/e2ee-remote.ts:345-389`). Meanwhile `rbox recover` is an explicit user-confirmed pin reset with a sequence-floor precheck (`src/cli/recover-cmd.ts:39-67`), not an automatic pull behavior. Decide whether automatic continuity-skipping is actually intended and obtain design-91-level threat-model agreement; otherwise make re-baseline an explicit recovery ceremony. Also specify equal-sequence/same-hash behavior, roster-at-commit verification, pin-write ordering, and how a pruned `commitsSince(0)` after `recover` is avoided.

5. **MAJOR — The client-state plan predates design 93's generation-CAS state writer and would permit torn or stale delta-base metadata if implemented as “three optional fields at save sites.”** The design treats `SyncState` and existing saves as direct field updates (`docs/design/84-manifest-delta-encoding.md:231-264`). Current state publication is a whole `StateSavePacket` guarded by stream, incarnation nonce, global sequence, per-repo generations, and lock ownership (`src/cli/config.ts:159-171,301-348`). Pull advances the global base only after filesystem and git application, and push publishes it via `saveStateSource` (`src/cli/sync.ts:343-346,705-725`). The manifest address/hash/chain-position tuple is correctness-critical and must be part of the same `packet.global` CAS unit as `sourceGlobalSeq` and `globalManifest`; it must never update on verify-only/pin advancement, partial apply, rejected packet, repo-pending projection, or mutex-degraded fallback. Revise the state schema and packet/application semantics explicitly, including migration and stream-reset behavior. This is also the mechanism that must preserve design 92's applied-base rule: push decisions use only `lastSyncedSequence`/`lastSyncedManifest`, never a verified-but-unapplied head (`src/cli/sync.ts:530-536`).

6. **MAJOR — The GC/accounting integration is stale after designs 95 and 24 and is not a “one-line union.”** The design cites old handler lines, says chain refs get “the same treatment, minus charging,” and describes clearing a prune marker (`docs/design/84-manifest-delta-encoding.md:304-335`). Current commit publication has inline-vs-sidecar carriers, receipt redemption, a `MAX_REFS_PER_COMMIT` accounting budget, and builds `shas` differently for each mode (`apps/api/src/workspace-sync.ts:263-341`). `CommitBodyView` and the signed engine `CommitBody` do not contain `manifestChain` (`apps/api/src/commit-envelope.ts:84-94`; `src/engine/e2ee/commit.ts:33-56`), and `roots()` has bounded retained-sequence/total-ref accounting plus sidecar fetch failure semantics (`apps/api/src/workspace-sync.ts:474-514`). More importantly post-95, `commitAccounting` can have the *whole super-batch* trigger-aborted by an R2 delete fence and returns all refs as `needsUpload` (`apps/api/src/commit-accounting.ts:185-193`); receipt minting itself fails closed while a delete fence is open (`apps/api/src/blobs.ts:49-78`). Specify whether chain links join accounting `shas` (and carrier/ref-count budgets), how already-entitled-but-Phase-1-marked links re-grant and clear `blob_ref_candidates` (`apps/api/src/commit-accounting.ts:169-182`), how a design-95 fence abort maps to retry without attempting to re-upload immutable historical links, and how `ROOTS_MAX_TOTAL_REFS` counts chain entries. Add the chain to the engine signer/parser, Worker parser, DO roots DTO, `versions.ts` reachable union, and all fail-closed tests; otherwise links can be omitted at one of several authoritative seams.

7. **MAJOR — Delta application does not explicitly preserve design 92's manifest-entry integrity and fail-closed carry contract.** Saying a `set` entry is “self-validating via `validateManifest`” (`docs/design/84-manifest-delta-encoding.md:162-190`) covers shape only. Current validation checks paths, tuple shapes, compression descriptors, collisions, and git sections (`src/engine/manifest-validate.ts:49-153`), while the landed producer path separately authenticates scanned `(sha256,size)` against encrypted bytes and defers churn rather than mutating the tuple (`src/cli/sync-recovery.ts:223-246,275-312`); push then builds the committed stable subset from the *applied* base (`src/cli/sync.ts:639-645`). The delta writer must diff the final `committed` manifest after deferral/schema stamping, not a scan/change stream, and the fast fold must not mutate the cached applied base before `resultHash`, full `validateManifest`, and filesystem apply succeed. Require tests for poisoned `set` tuples, compressed descriptor changes, deferred carry, verified-but-unapplied heads, and retry after apply-integrity failure—not only randomized structural fold equality.

8. **MAJOR — Phase 0 is already implemented and its proposed schema is stale, so the rollout cannot begin as written.** Current pull records `latest` details (`src/cli/sync.ts:218-227`) and current commit records details (`src/cli/sync.ts:673-682`) through the exact `recordDetails` channel (`src/engine/phase-report.ts:165-172`). The shipped commit timing is broader than design 84's `{encodeMs, encryptMs, uploadMs, encBytes}`: it includes account refresh, refset sidecar upload, and commit POST (`src/cli/e2ee-remote.ts:392-502`; formatter at `src/cli/sync.ts:93-95`). Therefore Phase A is measurement/analysis, not an instrumentation implementation phase, and “commit = encode + encrypt + upload” no longer closes the timer. Revise the decision arithmetic to include `refreshMs + sidecarMs + postMs` (and quantify residual/unattributed time), record current two-host v1.0.0+ measurements, and make the C1/C2 gate decision from those shipped fields. `src/cli/metrics.ts` only enables/constructs the report (`src/cli/metrics.ts:17-28`); the detailed data is emitted by `sync.ts`, which the design should cite.

9. **MINOR — Several mechanism/line claims have drifted enough to misdirect implementation.** Examples: manifest encode/upload is now `src/cli/e2ee-remote.ts:453-485`, decode is `:191-215`, the server conflict is `apps/api/src/workspace-sync.ts:343-390`, roots are `:474-514`, state fields are `src/cli/config.ts:94-129`, scan stamping is `src/engine/manifest.ts:91,198`, and the pack byte-bound helper is `src/cli/sync-git.ts:243-244` rather than the cited locations. Refresh every citation as requested, and update claims that `commitsSince` is “untouched”: adding `manifestChain` changes the signed commit body/types and every parser even if chain verification remains opaque. `docs/design/96-roots-index.md` is absent on this branch, so no design-96-specific constraint was reviewed; current design-95/current-main GC behavior is covered above.

10. **MINOR — Chain sizing and historical decode leave implementer choices that affect correctness and cost.** `MAX_MANIFEST_DELTA_CHAIN` remains an open decision despite being a wire/server acceptance limit (`docs/design/84-manifest-delta-encoding.md:217-225`); define whether the count includes head and snapshot and whether `manifestChain` includes the current `encManifestSha` (the prose currently says snapshot + intermediates, while I3 says every link). Define “bytes” as ciphertext bytes and how readers obtain trusted sizes. Also specify the primitive used to decrypt base links: current `openCommitHistorical` authenticates bytes against one commit's signed `encManifestSha` (`src/engine/e2ee/session.ts:376-399`), so it cannot be reused with the head commit for arbitrary base blobs. A chain decoder needs a bounded address-authenticated decrypt path using the signed `manifestChain`, with epoch selection and errors defined, and history/path-history should share folds rather than independently refetching the same chains.

**Final verdict: REVISE.** The core O(change) direction and the requirement to root every delta link are sound, but the design is not implementation-ready against current main. The head-authority repair deadlock, unenforceable completeness claim, and underspecified versioned wire format are blockers; the landed state-CAS, integrity, receipt-fence, roots-bounds, and already-shipped instrumentation paths require substantive revision before implementation dispatch.

## Round 1 — revision (Claude)

All ten findings accepted; the doc is rewritten (v3) against current main (incl. designs 91/92/93/95/96). Dispositions:

1. **BLOCKER (recovery deadlock) — accepted, redesigned.** §3.6.3 now specifies an explicit repair transaction: the chain-verified pin is the parent authority (`parentSequence = pin.commitSeq`, `parentCommitHash = pin.commitHash` — satisfies the applied-lag guard as written, no pin clearing, no remote-layer bypass); best-readable-ancestor recovery before superseding; guard ladder (mass-delete guard, daemon auto-repair only for a self-authored unreadable suffix, else explicit `rbox recover` chain-case ceremony that does NOT clear the pin); typed `ManifestChainError`; convergence argument (§3.5.2's 422→snapshot rule heals writer-side, §3.6.3 heals reader-side). "Snapshot on next push" text deleted; wedge anatomy documented with current line numbers.
2. **BLOCKER (I3 unenforceable) — accepted, restated + protocol added.** I3 split honestly (I3a server: syntactic/presence enforcement + rooting of what's LISTED; I3b reader: exact-match of the walked decrypted linkage against the signed `manifestChain` — order, terminal snapshot, every link, no extras, §3.6.1), yielding the composite invariant "accepted AND readable ⇒ fully rooted"; the buggy-signer residual is named and equated to publishing a garbage manifest (detected first pull → repair). Fast-path inductive obligation specified.
3. **BLOCKER (wire format) — accepted, fully specified.** §3.2: `rbox-mde1\n` magic-framed envelope (versioned discriminator; unknown `rbox-mde*` fails closed with upgrade message), strict single-line JSON header schema (unknown keys rejected), raw binary body (no base64), normative raw-v0 legacy rule, `bodyBytes` + streaming zstd cap (compression-bomb guard per design 79) + `MAX_MANIFEST_PLAINTEXT`/`MAX_ENVELOPE_HEADER`, one canonical-hash definition (JCS of decoded Manifest) with its O(N) verify cost stated honestly.
4. **MAJOR (re-baseline) — accepted.** §4.3 keeps the design-91 posture: no automatic continuity-skipping; recover stays the explicit ceremony. Current code behavior stated precisely (verifiedHead propagates NeedsRebaselineError; only the history helper catches it). The pruned-workspace recover gap (commitsSince(0) 409s after pin clear) is specified as a ceremony-scoped fallback (roster-verified head sig + retained-segment terminal match), with floor/equal-seq/equivocation and pin-write ordering spelled out.
5. **MAJOR (design-93 state) — accepted.** §3.4/I8: base metadata is `GlobalManifestMeta` INSIDE `StateSavePacket.global` (atomic with the manifest under the stream/nonce/global-seq CAS); never written on verify-only/decode-failure/rejected packet; degraded/forceLegacy and legacy writers drop it (fail-to-snapshot); resetSyncState wipes it; migration = undefined ⇒ snapshot trigger. Design-92 applied-base rule extended to the delta base.
6. **MAJOR (GC/accounting staleness) — accepted.** §3.5 rewritten: engine signer/parser + CommitBodyView touch list (§3.5.0, corrects "commitsSince untouched"); chain entries join the existing `shas` union through validateCommitRefs/commitAccounting (charging idempotence is the "minus charging" mechanics); marked-link and design-95 fence-abort 422s map to a snapshot retry (§3.3.5/§3.5.4 — never re-upload immutable links); ref budget includes the chain; design-96 roots v2 integration (refSetAt fold union, dropped_index lifecycle, gap `chainRefs`, versions.ts addRoot, FOLD_MAX_REFS/ROOTS_OUTER_MAX_REFS/MAX_UNIQUE_ROOTS math); §3.5.3 shows marker-clearing is inherited, not new code.
7. **MAJOR (design-92 carry) — accepted.** §4.6: encoder input is the final `committed`-vs-applied-base pair (post-defer/carry/schema-stamp), never a scan stream; fold purity + fold→resultHash→validate→apply→packet ordering; poisoned-tuple parity argument (apply-time caps still the gate) + dedicated §7.4 tests.
8. **MAJOR (Phase 0 shipped) — accepted.** §1.7 records the shipped channel (recordDetails via sync.ts/e2ee-remote.ts, PRs #161/#165); §5 rewritten as a measurement campaign over the shipped 7-field commit split incl. refresh/sidecar/post + residual; decision arithmetic restated over those fields; Phase A output = published per-host gate record.
9. **MINOR (citations) — accepted.** All citations refreshed against the rebased tree (e2ee-remote 191-216/227-259/392-511, workspace-sync 385-402/475-549/628-693, config 94-129/159-165/303-352, sync-git 241-244, etc.); design 96 now cited directly; "commitsSince untouched" corrected (§3.4 note + §3.5.0).
10. **MINOR (chain sizing/decode) — accepted.** §3.3.3/§3.5.0: `MAX_MANIFEST_DELTA_CHAIN` bounds `manifestChain.length` (excludes the head's own sha; fold depth = len+1); bytes = ciphertext bytes with the trust argument; new `openManifestChainBlob` address-authenticated decrypt primitive with epoch selection (target commit's signed keyEpoch; AAD as I4 backstop) and named per-link errors; §4.7 history folds share the chain walk/LRU.

## Round 2 — codex (sol)

I re-verified all ten Round-1 findings against the current tree, including the landed design-95 accounting fence and design-96 roots index. Findings 3, 6, 7, 8, and 9 are substantively resolved. Finding 1 has a sound parent-authority concept but an incomplete orchestration; findings 2, 4, 5, and 10 retain the issues below. The revision also introduces two new wire/state blockers.

1. **BLOCKER — The repair transaction normally exits through the existing no-op path without publishing its healing snapshot.** Section 3.6.3 says repair uses the standard push pipeline against the newest readable applied base and changes the parent/orchestration, but current push returns before `commit()` whenever the scan/git result equals that applied base (`src/cli/sync.ts:575-619`). That is the most likely repair case: the local tree has not changed; only the remote envelope is broken. Returning sequence A leaves broken head N authoritative, so the claimed convergence never occurs. Repair mode must explicitly bypass every pre-commit no-op/defer-empty exit and force a snapshot child of the pinned head even when its folded manifest is content-identical to the recovered base. Add that exact unchanged-tree case to the repair tests. Round-1 finding 1 is therefore not fully resolved.

2. **BLOCKER — The delta language cannot encode `manifestSchema`, so the promised deterministic fold is undefined on a live schema transition.** The header has no schema field and the only ops are file `set`/`del` and git `git-set`/`git-del` (§3.2). Nevertheless the fold rule says to “carry `manifestSchema` from the ops' folded result” and claims a schema change emits as part of the diff; there is no such source or op. This is not cosmetic metadata: `stampManifestSchemaForCommit` changes it (`src/cli/sync.ts:99-106`), and current validation requires schema ≥2 for `gitRepos`, ≥3 for pack chains, and ≥4 for compressed descriptors (`src/engine/manifest-validate.ts:117-149`). For example, a schema-3 base plus a compressed-entry change cannot fold to the schema-4 target or its `resultHash`. Add a canonical schema transition to the delta format, or require a snapshot whenever schema differs, and pin both directions (including removal if legal) in tests.

3. **BLOCKER — The epoch-boundary snapshot trigger has no persisted base epoch to compare, so I4 is not implementable from the specified state.** Section 3.3.2 requires comparing refreshed `keyEpoch`/`accountEpoch` with the base commit, but `GlobalManifestMeta` stores only `encManifestSha`, `manifestHash`, and aggregate `chainPos` (§3.4); current `SyncState` likewise has no applied-base epoch (`src/cli/config.ts:94-129`). The pin stores `accountEpoch` but not `keyEpoch`, and in the verified-but-unapplied case it describes the pin, not necessarily the applied delta base (`src/cli/e2ee-remote.ts:120-131`). If a rotation is missed, §3.6 decrypts old links under the target commit's new epoch and the new head is unreadable by construction. Persist the base commit's signed account/key epochs atomically with its manifest metadata (or specify an authenticated lookup that works when applied state lags the pin) and test rotation from an applied older base. This is a new soundness hole.

4. **MAJOR — Phase D still cannot perform I3's exact signed-list check from the proposed fast-path state.** Full verification requires the walked ordered addresses to equal `manifestChain` exactly (§3.6.1), but `chainPos` retains only `{snapshotEncSha, links, bytes}` and the fast path fetches only the head (§3.4). Those aggregates cannot prove that the new list equals the previously verified list plus the immediate base. A signer can substitute or reorder an intermediate address while keeping the same snapshot, count, and immediate `baseEncSha`; the cached fold succeeds, the wrong addresses are rooted, and a required older link can later be collected. Persist the exact bounded verified chain (or an authenticated commitment sufficient to compare the exact list), or perform a full verification fetch. Add omitted/extra/reordered tests specifically through the fast path, not only the cold walk. Round-1 finding 2 remains open on this path.

5. **MAJOR — `manifestMeta` is still incompatible with design-93's repo-pending projection, despite I8 claiming otherwise.** Current packet application stores the file-only global and reconstructs `lastSyncedManifest.gitRepos` from per-repo generation records (`src/cli/config.ts:329-345`); an unapplied remote repo can remain in `pending` while its applied `base` stays older. The resulting persisted `lastSyncedManifest` is intentionally not the fully folded remote manifest. Yet §3.4 says every successful pull stores the head's full folded hash/address in `manifestMeta`, and the proposed packet has only one global meta slot. That can make the meta certify a manifest different from the object beside it, poisoning both the delta writer and fast fold. Define the exact suppression/clearing rule when any repo is pending (forcing snapshots), or persist a separate full remote folded base distinct from the applied/projected manifest. Test a pull with one pending repo followed by push and by a fast-path pull. Round-1 finding 5's atomicity fix is necessary but not sufficient.

6. **MAJOR — The revised equal-sequence recover/equivocation claim is false with the current pin-clearing ceremony.** Section 4.3 says equal sequence may proceed and that the post-clear pull's equivocation checks reject a different hash. Current recover checks only `serverHead >= pin.commitSeq`, then clears the pin (`src/cli/recover-cmd.ts:59-67`). Once cleared, `verifiedHead()` starts at genesis and has no old same-sequence hash to compare (`src/cli/e2ee-remote.ts:227-259`); a different, otherwise valid genesis-to-head chain at the same sequence is therefore not rejected by the cited checks. Preserve the old pin until the replacement head/retained segment has been verified under the ceremony, or explicitly bind the fetched same-sequence head to the pre-clear hash. Round-1 finding 4 remains partially unresolved.

7. **MINOR — Two stated bounds remain policy placeholders rather than implementable protocol rules.** `MAX_MANIFEST_DELTA_CHAIN` is still an “open decision” with only a proposal of 16 (§3.3.3, §10.1), although client parsing, server acceptance, roots budgets, and interoperability require one normative shared value; this leaves Round-1 finding 10 incomplete. Separately, the retention trigger (§3.3.7) gives no retention-floor input or predicate available to the commit path, so it cannot provide the advertised defense against a future retention change. Fix the shared constant before dispatch, and either specify a concrete retention contract/predicate or remove that trigger and prove the fixed chain cap is within the minimum retained window.

Verdict: REVISE

## Round 2 — revision (Claude)

All seven findings accepted (v4):

1. **BLOCKER (repair no-op exit) — accepted.** §3.6.3 adds "the commit is unconditional": repair mode bypasses the `filesUnchanged && gitUnchanged` short-circuit (sync.ts:575-619) and the everything-deferred short-circuit (:652-658); a content-identical snapshot child of the pinned head IS the healing artifact. Unchanged-tree repair is a named §7.4 test.
2. **BLOCKER (manifestSchema fold undefined) — accepted.** The delta header gains `manifestSchema?` carried VERBATIM (same I5 rule as `generatedAt`; absent ⇒ folded manifest has no schema key), bounded by `KNOWN_MANIFEST_SCHEMA` at the header; fold stamps it, never re-derives; `validateManifest` on the fold enforces sufficiency (≥2 gitRepos / ≥4 compressed). Both transition directions (3→4, 4→absent) wire-encodable + tested. I5 restated over both carried scalars.
3. **BLOCKER (no persisted base epoch) — accepted.** `GlobalManifestMeta` gains the base commit's SIGNED `accountEpoch` + `keyEpoch` (pull: from the applied head's parsed body; push: the D1-checked write context), atomic with the manifest under the packet CAS. §3.3.2 rewritten: the I4 trigger compares the write context against the META epochs (local check, every delta-eligible commit), explicitly not the pin (which lacks keyEpoch and may describe an unapplied head). Rotation-from-older-applied-base is a named test.
4. **MAJOR (fast-path exact-list) — accepted.** `chainPos` aggregates replaced by `meta.chain: string[]` (the base's exact list-verified `manifestChain`, ≤16×64hex) + `chainBytes`. Fast path now requires the head's signed list === `meta.chain + [meta.encManifestSha]` element-wise (inductive soundness argued in §3.6.1); intermediate substitution/reorder with preserved snapshot/length/immediate-base fails; mismatch demotes to the cold walk. Fast-path list-attack tests added to §7.4.
5. **MAJOR (repo-pending projection) — accepted.** §3.4 gains the suppression rule: meta lands iff the post-application projection equals the described manifest — the only divergence channel is the gitRepos reconstruction (file layer stored verbatim), so the check is a cheap deep-equal (no pending repo, no newer-sourceSeq retention); divergence ⇒ meta undefined (clears prior) ⇒ snapshot path until a fully-applied pull. Push side covered too (gitBaseAfterCommit keeps old bases for pending repos ⇒ suppression). Pending-repo pull→push→fast-path test added.
6. **MAJOR (equal-seq recover) — accepted.** §4.3 rewritten: the ceremony RETAINS the prior pin until the replacement chain/head verifies; floor rule compares against the retained pin (`>` or equal-seq-same-hash; different hash refused as equivocation); the pin is overwritten in one save — no cleared window. The false "post-clear equivocation checks" claim removed.
7. **MINOR (placeholder bounds) — accepted.** `MAX_MANIFEST_DELTA_CHAIN = 16` normative (one shared engine constant across client trigger/decode/Worker gate; §10.1 reduced to post-soak retuning). The retention trigger is REMOVED with the proof it was vacuous: link availability is independent of the retention floor (prune drops seq pointers, never listed blobs; links are fetched by address from the signed list), and the real guarantees are §3.5.5 roots + §3.3.5 snapshot fallback.

## Round 3 — codex (sol)

I re-verified all seven Round-2 findings against the current design and the current tree (including designs 95 and 96). Findings 1–7 are substantively resolved: repair is unconditional across both early exits; `manifestSchema` is carried verbatim; signed base epochs and the exact verified chain are persisted; repo-projection divergence suppresses meta; recover retains and compares the old pin; and the chain cap/rooting proof are now normative. The revision nevertheless leaves the following new implementation holes.

1. **MAJOR — The byte-bound snapshot trigger still has no persisted terminal-snapshot size, so it cannot be evaluated after the first delta.** Section 3.3.4 requires comparing cumulative delta ciphertext bytes against “the base snapshot's ciphertext bytes,” but the revised `GlobalManifestMeta` in §3.4 stores only `chainBytes`; it has no `snapshotBytes`. A snapshot base records `chain: []` / `chainBytes: 0`, and after one or more deltas neither `lastSyncedManifest` nor the signed `manifestChain` carries any blob length. The current state likewise has no such source (`src/cli/config.ts:94-129`), and content-addressing authenticates a fetched length but does not make that length locally available without fetching the terminal snapshot—defeating the steady-state fast path. Persist the terminal snapshot ciphertext size atomically in `GlobalManifestMeta` (and propagate it unchanged across deltas), or explicitly fetch it before the decision; define whether the threshold includes the head being proposed; and add restore/migration/fast-path tests for the trigger. As written, one of the mandatory compaction rules is not implementable.

2. **MAJOR — Repo-only packet transitions are required to clear `manifestMeta`, but the specified packet shape provides no operation that can do so.** Section 3.4 correctly notices that a repo-only packet which changes a repo `base` invalidates meta, then says it clears a present meta. But the proposed type places `manifestMeta?: GlobalManifestMeta` only inside `packet.global`, while a repo-only packet has no `global`; the current application path leaves all global fields untouched in that case (`src/cli/config.ts:329-345`). Merely omitting an optional field cannot distinguish “preserve” from “clear,” and synthesizing a global member would incorrectly participate in the global sequence/CAS path and needs a truthful file manifest. Specify an explicit packet-level clear operation (or a normative rule inside `applyStateSavePacket` that clears meta iff an accepted repo transition changes the projected base), including its generation-CAS behavior and legacy fallback. Test an accepted repo-only base change, a repo-generation rejection, and a newer-sourceSeq retained transition; otherwise stale meta can survive beside a changed projected manifest and poison the next fast fold/delta.

3. **MINOR — Two decoder/interoperability bounds are simultaneously protocol constants and unresolved proposals.** Section 3.2 normatively names `MAX_MANIFEST_PLAINTEXT = 512 MiB` and `MAX_ENVELOPE_HEADER = 64 KiB`, and the acceptance tests require those bounds, but §10.8 still calls both values “Proposed.” Readers and writers must agree on these limits before Phase B/C; differing choices can make an honestly written envelope unreadable, and the plaintext cap is part of the compression-bomb posture. Resolve the values in the contract (leaving only coordinated post-soak retuning open), as was done for `MAX_MANIFEST_DELTA_CHAIN`.

Verdict: REVISE

## Round 3 — revision (Claude)

All three findings accepted (v5):

1. **MAJOR (byte-bound trigger unimplementable) — accepted.** `GlobalManifestMeta` gains `snapshotBytes` (the terminal snapshot's ciphertext length, recorded from an observed length — writer's upload or cold-walk fetch — and propagated unchanged across deltas; the fast path never refetches to size it). §3.3.4 rewritten in implementable form: encode+encrypt the candidate delta first (O(change)), fire when `chainBytes + candidateDeltaCipherBytes ≥ snapshotBytes` (threshold explicitly includes the proposed head), discard the delta and re-emit as snapshot. Migration/restore: absent/legacy meta ⇒ §3.3.1 snapshot path. Tests added (§7.4).
2. **MAJOR (repo-only packet cannot clear meta) — accepted.** Normative rule INSIDE `applyStateSavePacket` (not the packet shape): after accepted repo transitions fold, a packet with no `global` whose accepted transitions changed any record's projected `base` clears the persisted meta in the same atomic write; rejected packets change nothing; retained newer-`sourceSeq` transitions preserve (base unchanged); legacy/forceLegacy writers drop meta unconditionally (subsumes the rule off the fenced path). Tests: accepted repo-only base change / repo-generation rejection / retained transition.
3. **MINOR (bounds as proposals) — accepted.** `MAX_MANIFEST_PLAINTEXT = 512 MiB` and `MAX_ENVELOPE_HEADER = 64 KiB` are normative shared protocol constants for B/C (writers refuse to emit past them, so honest envelopes are readable by construction); §10.8 reduced to post-soak retuning, read-side first.

## Round 4 — codex (sol)

I re-verified every Round-1 through Round-3 finding against design v5 and the current tree, including the landed design-95 delete fence/accounting path and design-96 roots-v2 index. The prior findings are substantively resolved: the repair transaction has a usable pinned parent and bypasses both early exits; I3 is honestly split and exact-list checked on cold and fast paths; the envelope, schema carry, epoch boundary, packet-CAS placement, repo-projection suppression/clear, roots/accounting integration, chain and decoder bounds, recover ceremony, `snapshotBytes`, and proposed-head byte trigger are now specified. One new fail-to-snapshot gap remains.

1. **MINOR — “legacy meta shape ⇒ snapshot” has no runtime validation/normalization contract, so the new byte trigger can fail open through JavaScript coercion.** Section 3.3.4 says any state with a legacy meta shape has no base and snapshots, and §7.4 requires an absent/legacy-meta test (`docs/design/84-manifest-delta-encoding.md:362-363,1127-1132`), but §3.4 supplies only a TypeScript interface. Current state loading is an unchecked `JSON.parse` cast (`src/cli/config.ts:191-200`), so an old/partial object with (for example) no `snapshotBytes` remains truthy and `chainBytes + candidateDeltaCipherBytes >= undefined` evaluates false via `NaN`; it does not automatically take §3.3.1. Specify a runtime validator at state load or delta-base selection: hashes and chain entries must be 64-hex; the chain must be duplicate-free and length ≤16; epochs and byte counters must be bounded safe integers (`snapshotBytes > 0`, `chainBytes >= 0`, with snapshot-base consistency); any missing/invalid field must normalize the whole `manifestMeta` to `undefined` before epoch checks, fast-fold matching, or byte arithmetic. Add malformed and partial persisted-meta cases, not merely `undefined`, to the migration/restore test.

Verdict: REVISE

## Round 4 — revision (Claude)

Finding accepted (v6):

1. **MINOR (no runtime meta validation) — accepted.** §3.4 gains a normative `validManifestMeta(v)` validator gating EVERY consumption point (delta-base selection, §3.3.2 epoch trigger, fast-fold match, §3.3.4 byte arithmetic): 64-hex hashes; chain ≤ MAX_MANIFEST_DELTA_CHAIN, unique, head-sha excluded; epochs non-negative safe integers; `snapshotBytes` positive safe integer; `chainBytes` non-negative safe integer with `chain.length === 0 ⇒ chainBytes === 0`. Any missing/invalid field normalizes the WHOLE meta to `undefined` (fail-to-snapshot / cold walk) before any coercible arithmetic runs — the `>= undefined ⇒ NaN ⇒ false` fail-open is named as the motivating case. §3.3.4's migration note now routes through the validator, and §7.4 tests partial/malformed metas (missing snapshotBytes, non-hex hash, over-cap/duplicate/head-in-chain, negative/NaN counters, chainBytes inconsistency) at every consumption point, not merely `undefined`.

## Round 5 — codex (sol)

I re-verified every Round-1 through Round-4 finding against design v6 and the current tree, including the landed design-95 delete-fence/accounting path and design-96 roots-v2 index. The prior findings are substantively resolved: repair authority and no-op bypass, exact signed-chain verification, envelope/schema/epoch rules, packet-CAS metadata and repo-projection invalidation, recover ceremony, decoder and chain bounds, roots/accounting integration, persisted sizing inputs, and runtime meta normalization are now specified. Two new gaps remain.

1. **MAJOR — The bounded 422 response can hide an unsatisfied manifest-chain link, so the required snapshot fallback is not implementable from every server response.** Section 3.5.4 requires the client to test `missing ∩ manifestChain` and snapshot whenever that intersection is nonempty. Current `unsatisfiedBlobsBody`, however, returns only the first `MAX_MISSING_SHAS_RESPONSE = 10_000` entries and reports the full count separately as `missingTotal` (`apps/api/src/commit-envelope.ts:27-39`). The proposed receipts union orders the manifest carrier/sidecar/data refs before `...manifestChain` (§3.5.2), so a commit with more than 10,000 unsatisfied earlier refs can also have an unsatisfied historical chain link that is absent from the bounded `missing` array. The client then classifies only the visible data refs for re-upload/re-receipt instead of taking the mandatory snapshot path; because it does not retain historical chain ciphertext, retries can make no progress and can repeatedly reproduce the same truncated response. Specify an unambiguous bounded contract: for example, return a separate `chainMissing` flag/list, prioritize all (at most 16) chain misses ahead of data misses, or require a conservative snapshot whenever `missingTotal > missing.length` and the attempted commit has a nonempty chain. Cover the truncation boundary on both `validateCommitRefs` and delete-fence `commitAccounting` 422 paths.

2. **MINOR — `validManifestMeta` still accepts an impossible non-snapshot chain with `chainBytes === 0`, allowing corrupted persisted state to undercount mandatory recompaction.** The new validator requires only `chain.length === 0 ⇒ chainBytes === 0` (§3.4). But after any delta, `chain` is nonempty and `chainBytes` includes at least that delta's positive ciphertext length; therefore a hand-edited or partial meta with a nonempty well-formed chain and zero bytes is invalid by construction. It currently passes every consumption gate, remains delta-eligible, and makes §3.3.4's `chainBytes + candidateDeltaCipherBytes` omit all prior delta bytes, delaying or defeating the byte-bound snapshot trigger that the validator was added to protect. Require `chainBytes === 0` iff `chain.length === 0` (at minimum `chain.length > 0 ⇒ chainBytes > 0`) and add this inverse-consistency case to the malformed-meta tests.

Verdict: REVISE
