{
  "name": "@rbox/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "rbox sync engine (Phase 1: locally-testable core)",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit && tsc -p apps/api --noEmit"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "ignore": "^5.3.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260626.1",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.4"
  }
}
