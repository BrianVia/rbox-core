{
  "name": "@rbox/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "rbox sync engine (Phase 1: locally-testable core)",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ignore": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.4"
  }
}
